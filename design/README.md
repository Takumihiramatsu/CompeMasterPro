# design/ — UI再設計のデザイン資料

**アプリではありません。** 画面の見本（静的な絵）です。順位計算・馬券・保存などの機能は入っていません。
アプリの本体は `docs/pc.html` だけです。GitHub Pages が公開するのは `docs/` なので、ここは公開ページに出ません。

| ファイル | 内容 |
|---|---|
| `CompeMasterPro-Redesign_dc.html` | 再設計案 第2版（「いつ・何を入れるか」で画面を組み直す）。ブラウザで直接開く |
| `support.js` | 上の資料を表示するための実行部品（自動生成。**手で直さない**） |

2つは同じフォルダに置いてください。資料が `./support.js` を読みます。

## 経緯と取り込み状況

`HANDOVER.md` の「デザイン案の判断（2026-09-16）」と「4.7 デザインからの引き継ぎ」以降を読んでください。
採ったもの・採らないと決めたもの（配色、外部フォント、大会一覧、作成ウィザード）と、その理由が書いてあります。
**同じ提案が来たら、まずそこを読み返します。**

## 個人情報

見本の氏名は架空です。会場名も架空の名前（さくら台カントリー倶楽部 など）に置き換えてあります
（2026-09-24。届いた版には実在のゴルフ場名と開催日が並んでいました）。
`tests/privacy.js` の「9. 大会と会場が結び付いていないか」がこのフォルダの `.html` と `.md` も検査します。

**画面の画像（スクリーンショット）はここに置きません。** 画像の中の文字は置き換えも検査もできないためです。
