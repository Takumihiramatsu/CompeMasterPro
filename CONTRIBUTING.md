# 開発の手引き

このリポジトリで開発を引き継ぐ人へ。**この1枚を読めば作業を始められます。**

必要なのは **Node.js** と、テキストエディタだけです。ビルドもパッケージのインストールもありません。

---

## 1. 何を作っているか

ゴルフコンペの運営システムです。参加者の登録から組み合わせ、スコア集計、
予想ゲームの精算、表彰式の演出までを1つで行います。年に1回、実際の大会で使います。

**`docs/pc.html` の1ファイルがアプリの全体です。** HTML・CSS・JavaScriptがすべてこの中に入っています。

```
CompeMasterPro/
├─ docs/                  ← 配布物。GitHub Pages がここを公開する
│   ├─ pc.html            アプリ本体（これがすべて）
│   ├─ index.html         pc.html への転送ページ
│   ├─ sw.js              オフライン用のキャッシュ
│   ├─ pc.webmanifest     ホーム画面への追加用
│   ├─ icon-*.png / apple-touch-icon.png
│   └─ README.md          配布のしかた（利用者向け）
├─ tests/                 ← 検証スイート。28本・2338項目
│   └─ *.js
├─ design/                ← UI再設計のデザイン資料（アプリではない。公開されない）
└─ CONTRIBUTING.md        この文書
```

---

## 2. 最初にやること

```bash
git clone https://github.com/Takumihiramatsu/CompeMasterPro.git
cd CompeMasterPro/tests
for f in *.js; do echo "== $f"; node "$f"; done
```

**`NG` が0で、`OK` の合計が2338であれば準備完了です。** 何もインストールする必要はありません。

**「NGが0」だけで判断しないでください。** 1本が途中で例外になると「合計 NG」が出ないまま止まります
（2026-09-16に `money.js` で発生）。OKの合計が2338に届くことまで確かめます。

```bash
for f in *.js; do node "$f"; done 2>&1 | grep -cE '^\s*OK\b'   # → 2338
```

アプリを触るときは `docs/pc.html` をブラウザで直接開いてください。サーバーは要りません。
画面右上の「データ」タブから、サンプルの大会を読み込めます。

---

## 3. 変更の手順

```
1. docs/pc.html を編集する
2. tests/ で全部走らせる          ← NGが0であることを確認
3. 公開前に node privacy.js       ← 個人情報が混ざっていないか
4. docs/sw.js の VER を1つ上げる  ← ここを忘れると利用者に古い画面が残る
5. コミット
```

### 4番を忘れないでください

```js
const VER = "compe-v68";   // ← 番号を必ず上げる
```

サービスワーカーはキャッシュ優先で動きます。**`VER` を変えないと、利用者の端末に
いつまでも古い版が残ります。** CSSや画面の見た目を変えたときは、番号を上げても
残ることがあるので、スマホはホーム画面のアイコンを削除して追加し直してもらいます。

---

## 4. 変えてはいけない決めごと

**理由を確認せずに変えないでください。**どれも実際の失敗から決まったものです。

| 決めごと | 理由 |
|---|---|
| **HTMLファイル1つ。外部ライブラリもCDNもゼロ** | ゴルフ場は電波が弱い。USBで持ち運べることが最優先。npmを導入した時点でこの設計は壊れます |
| **文字認識（OCR）は積まない** | 5〜20MBになり単一ファイルが壊れる。精度も端末標準に及ばない。2026-09-04に再検討して却下済み |
| **スコアは打ち込んで検算する** | 「正しいと確認できること」が安心の中身であって、認識精度を上げることではない |
| **画面を作り直す処理は入力確定時だけ** | `oninput` で `render()` を呼ぶと日本語変換が壊れる。過去に2度発生 |
| **自動保存はlocalStorage、保存の主役はファイル書き出し** | 端末をまたげないため。自動保存は事故防止の保険 |
| **配布物に実名を入れない** | 公開リポジトリに実名が載る事故が2026-09-04に発生 |

---

## 5. 過去に踏んだ落とし穴

同じ穴に落ちないように。**すべて再現テストとして `tests/` に残してあります。**

1. **`oninput` で `render()` を呼ぶと日本語が打てない** — 確定時（`onchange`／`compositionend`）に変更
2. **Enterでの候補選択が、変換確定のEnterを拾う** — 機能ごと撤去
3. **施設を切り替えたとき9Hの番号が範囲外のまま残る** — `mFix()` で毎回補正
4. **`normalize("NFKC")` が氏名の全角スペースを半角にする** — 一時退避してから正規化
5. **Google Fontsの読み込みでオフラインが壊れる** — 端末内の明朝体に置換
6. **集計表の解析がOUTの小計をグロスと誤認** — 先頭から数値を拾っていた。末尾から検算する方式へ
7. **CSSの後勝ちの上書きに気づかず修正が効かない** — テストは「最後に勝つ値」を見る
8. **表の列に幅の指定がないと1文字ぶんまで潰れる** — 文字の入る列すべてに下限幅
9. **「取れた数が多いほう」で判断すると誤った結果を採る** — 検算を通った側を優先する
10. **`MASTER=AC.createGain()` と書いてしまい、音が一度も鳴っていなかった** —
    `MASTER` はゴルフ場マスタの `const` 配列。代入で例外になり、
    `draw()` がそれを受け止めていなかったため進捗表示も止まっていた

**10番が起きたのは、テストが音の経路を一度も通っていなかったからです。**
`window={AudioContext:function(){throw 0}}` という器で動かしており、
最初の1行で例外になってその先へ到達していませんでした。961項目あっても、
通らない経路の不具合は見つかりません。

---

## 6. 検証スイートの読み方

`tests/` の28本は、それぞれ独立して動きます。器（`document` や `window` の代わり）を
自前で用意し、`pc.html` の `<script>` を `new Function()` で読み込んで直接呼びます。

**要点となる7本です。**

| ファイル | 何を保証するか |
|---|---|
| `sheet.js`（132） | スコア入力の中心。打ち込みの検算、氏名なしの貼り付け、3通りの読み方 |
| `rules.js`（102） | 賞のルール全般と、対象ホールがパー3／パー5と噛み合っているかの警告 |
| `haiya.js`（62） | **2026年の実際の大会を再現し、16名の全項目が公式値と一致すること** |
| `sound.js`（36） | 表彰式の音。34画面の通し再生と、音が壊れても止まらないこと |
| `void.js`（61） | 無効になった予想の返金と、得票からの除外 |
| `privacy.js`（25） | **配布ファイルに個人情報が混ざっていないこと。リポジトリ全体で大会と会場が結び付いていないこと** |
| `backup.js`（42） | **実データがあるまま読み込む・新規作成する・全消去すると無確認で失われないこと。世代の控え（最大3件）とCSVのエスケープ** |

### テストの書き方の約束

- **不具合を直したら、必ず再現テストを足す。** 「直した」だけでは同じ穴に落ちます
- **書いてあるかではなく、効いているかを見る。** CSSは「最後に勝つ値」を、
  音は「発振器が実際に作られたか」を確かめます
- **直す前のファイルに当てて、本当に落ちることを確かめる。** 落ちないテストは無意味です

### `haiya.js` の氏名について

**氏名は架空のものに置き換えてあります。数字（スコア・HDCP・ネット・配当・罰金）は
2026年の実物のままです。** 実名との対応表は社内限りで、このリポジトリにはありません。

---

## 7. 公開前の確認

```bash
node privacy.js
```

`docs/` に実在の氏名・生年月日・組織名が混ざっていないかを検査します。
あわせて**リポジトリ全体**（`tests/`・文書・`pc.html`）で、内蔵マスタのゴルフ場名や略称（◯◯CC）が
`OKCLUB`／`OKSTEM` に理由つきで並べた語だけであることを確かめます。
**テストでゴルフ場を例に使うときは、大会と無関係なコースを選び、理由を添えて足してください。**
**公開リポジトリなので、これを通さずにコミットしないでください。**

このテストは「実名リストと照合する」のではなく「**素性を説明できる語しか
置かれていないこと**」を確かめます。テスト自体が実名の一覧になるのを避けるためです。
日付には1つずつ理由を書かせています。新しく日付を足すときは、
`privacy.js` の `OKDATE` に理由を添えてください。

---

## 8. GitHub Pages について

`docs/` が `https://takumihiramatsu.github.io/CompeMasterPro/` として公開されています。

**リポジトリを非公開にすると、無料プランでは Pages が止まります**（URLが404になります）。
2026-09-03 と 09-04 に実際に起きました。非公開にする必要が出たときは、
配布方法をあらかじめ決めてから切り替えてください。

---

## 9. 用語

ゴルフの用語が分からなくても開発はできますが、テストを読むときに必要になります。

| 語 | 意味 |
|---|---|
| **グロス** | 実際に打った打数の合計 |
| **HDCP（ハンディ）** | 実力差を埋めるために引く数。ペリア方式で自動計算する |
| **ネット** | `グロス − HDCP`。この順位で優勝を決める |
| **ダブルペリア** | 隠しておいた12ホールの成績からHDCPを出す方式。`(隠し12の合計 × 1.5 − コースパー) × 0.8` |
| **馬券** | ネット上位2名が入る「枠」を当てる予想ゲーム |
| **GTO** | グロス下位3名を当てる予想ゲーム |
| **ニアピン／ドラコン** | パー3でピンに近づけた人／パー5で最も飛ばした人への賞 |

---

## 10. 困ったとき

- **アプリの全体像**は `docs/README.md`（利用者向け）を読んでください
- **なぜそうなっているか**は、`docs/pc.html` のコメントに書いてあります。
  とくに過去の失敗については、その場に理由が書いてあります
- **テストが落ちたとき**は、まず「テストが正しいか」を疑ってください。
  実装が正しくてテストの期待値が誤っていた例が何度もあります
