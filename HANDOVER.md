# CompeMaster Pro 引き継ぎ

**ゴルフコンペ総合運営システム｜2026-09-21 時点（ラッキー賞の表示名・発表スライドの非表示バグ・v66）**

新しいチャットで作業を続けるための引き継ぎです。この1枚を読めば、いま何がどこにあり、次に何をすべきかが分かります。

> **氏名は架空のものに置き換えてあります。** 数字（スコア・HDCP・ネット・配当・罰金）は
> 実物のままなので、記録としての価値は変わりません。実名との対応表は社内限りで、
> このリポジトリにはありません。
>
> **9月4日に方針が大きく変わりました。**
> スマホ用の読み取りアプリを**廃止**し、スコア入力は「打ち込んで1行ずつ検算する」形を正規手順にしました。
> 経緯は「4. 読み取りアプリを廃止した理由」に書いてあります。**同じ検討を繰り返さないため、必ず読んでください。**
>
> **作業を始める前に、必ず `git clone` で最新を取ってください。**
> 9月8日、別のアカウントで加えられた変更（`fPair2` と `hscroll`）に気づかず
> 上書きしかけました。差分を突き合わせて助かりましたが、
> **手元のコピーから作業を始めると、他の作業が消えます。**

---

## 0. 2026-09-21 の修正（v66・ラッキー賞の表示名／発表スライドの非表示バグ）

動画2（ローンチ動画）の撮影準備で実機を確認していて見つかった2件。どちらもバグ修正で、
既存データへの影響はない（後方互換の変更は不要）。

**1. 「抽選賞」の表示名が「ラッキー賞」になっていなかった**

内部キーは`DB.meta.use.lucky`／`PZ().lucky`など一貫して`lucky`（ラッキー）なのに、
画面表示の文字列は11箇所すべて「抽選賞」に固定されていた。設計書（§9のペルソナ説明）や
マーケティング側の資料では一貫して「ラッキー賞」と呼んでいたため、実装時にどちらかへ
統一し忘れたとみられる。

対処：画面表示の「抽選賞」を11箇所すべて「ラッキー賞」に置換（`pc.html`）。内部キー・
関数名（`luckyPool`・`drawLucky`・`LK()`など）は変えていない。`tests/rules.js`の期待値
（収支の支出・発表スライド内のHTML照合）2箇所も「ラッキー賞」に合わせて更新。

**2. 大波賞・おしどり賞・早出賞などの受賞者が、たまたま上位者と同じ人だと発表スライドが消えていた**

`buildSlides()`の「賞ごとに1枚ずつ発表する」処理（2026-09-08導入）が、対象を絞る条件を
**受賞者の氏名**で判定していた（`!st.net.slice(0,3).some(x=>x.n===r.who) && !(st.best&&st.best.n===r.who)`）。
これは本来「優勝・2位・3位・ベスグロそのものの賞（既に専用スライドが出ている）を除く」
つもりだったはずが、結果として「**どの賞であれ**、受賞者が上位3名かベスグロの人と
同一人物なら丸ごと非表示」になっていた。

実機確認（`persona01`相当のサンプルデータに大波賞・おしどり賞・早出賞を追加）で、
おしどり賞の受賞者がたまたまネット2位の人、早出賞の受賞者がたまたま優勝者（兼ベスグロ）と
同一人物だったため、発表画面に2つとも一切出てこなかった（大波賞は受賞者が上位3名・
ベスグロのいずれとも別人だったため、問題なく表示された）。賞金・収支画面には受賞者名が
正しく表示されるため、「発表本番でだけ賞が消える」という、幹事が事前に気づきにくい形の
不具合だった。

対処：判定を**受賞者の氏名**ではなく**賞の種類（`kind`）**に変更。除外するのは
`kind==="best"`（ベスグロ）と`kind==="rank"`かつ`n<=3`（優勝・2位・3位）の行だけとし、
それ以外の賞（大波・小波・おしどり・早出・猛打・ブービー・手で指定など）は、
受賞者が誰であっても必ず個別スライドで発表されるようにした。

**確認した非対象（壊していないこと）**：「賞がたくさんあるときだけ後半をまとめて1枚にする」
仕組み（`ONE=5`、6件目以降を「その他の賞」1枚にまとめる）はそのまま。優勝・2位・3位・
ベスグロの専用スライド自体（`s-1`〜`s-3`・`s-best`）にも手を入れていない。テストスイート
（27本・全NG 0）で確認済み。`docs/sw.js`・`docs/README.md`・`CONTRIBUTING.md`の版番号は
compe-v65 → **compe-v66**に統一。

---

## 0-1. 2026-09-21 の追加（v65・データ事故対策3件）

外部評価（ChatGPTによるレビュー）をきっかけに、実際にコードを確認して裏を取れた指摘だけ
3件まとめて対応した（「点数」評価そのものは根拠不明のため採用していない。詳細は
このセッションでのユーザーとの検討過程を参照）。

**1. 読み込み・新規作成・全消去の前に、未保存の内容を無確認で失っていた**

`upJson()`（ファイルの読み込み）には、いまの大会に実データ（参加者・馬券・GTOのいずれか）が
あっても確認が一切なく、誤ったファイルを選ぶと今の入力がそのまま消えていた。`newCompe()`
（新しい大会）・`wipe()`（全部消す）にはもともと確認があったが、`upJson()`だけ抜けていた。

対処：3つの操作すべてで、実データがあるときだけ`confirm()`を挟む（`hasRealData()`という
共通の判定関数を新設し、`newCompe`・`copyPrev`の重複していた判定もこれに統合）。

**2. 自動保存が「直近1件」のみで、世代管理が無かった**

`localStorage`の自動保存（`LSKEY`）は前から1件だけを常に上書きする方式で、これは変えていない
（起動時の自動復元など、既存の挙動・テストへの影響を避けるため）。これとは別に、
以下の2つの場面で「世代」を最大3件まで積む仕組み（`GKEY`、`pushGen()`/`loadGens()`）を追加した。

- 前回の世代保存から5分以上経っていて、かつ実データがある（`maybeAutoGen()`。自動保存の
  400msデバウンスに相乗り）
- 大会をまるごと置き換える操作（読み込み・新規作成・全消去）の直前（1と同じ`hasRealData()`判定）

「データ」タブに、控えの一覧と復元ボタン（`restoreGen(i)`）・一括削除（`clearGens()`）を追加。
控えが1件も無いときは何も表示しない。

**3. CSV出力（精算一覧）が未エスケープだった**

`dlCsv()`が`rows.map(r=>r.join(","))`のままで、氏名や団体名にカンマ・引用符・改行が
入っていると列がずれる作りだった。`csvCell()`を新設し、該当する値だけダブルクォートで囲む
（Excel等が読める一般的な形）。

**確認した非対象（壊していないこと）**：参加者がいない・馬券もGTOも無い「白紀の状態」から
読み込む・新規作成する場合は、これまでどおり無確認で進む（`hasRealData()`がfalseのときは
`confirm()`を呼ばない）。壊れたファイルを読み込んだときは、確認より前に「読み込めませんでした」
で止まる（従来どおり）。既存の自動保存（`LSKEY`）・起動時の復元の挙動は一切変えていない。

影響を受けたファイル：`docs/pc.html`（`hasRealData`・`pushGen`・`loadGens`・`maybeAutoGen`・
`restoreGen`・`clearGens`・`csvCell`の追加、`upJson`・`newCompe`・`copyPrev`・`wipe`・`dlCsv`・
`vData`の変更）、`docs/sw.js`（VER: v64→v65）、`docs/README.md`・`CONTRIBUTING.md`・
`README.md`（バージョン表記・検証スイートの本数と項目数を同期。あわせて`CONTRIBUTING.md`内で
古いまま残っていた「OKの合計2179」という記載も実数に合わせて修正）、`tests/backup.js`
（新規、42項目）、`tests/privacy.js`（`OKDATE`に2026-09-21を追加）。
テストは27本・2316項目、NG 0（前回2274項目から+42）。

---

## 0-2. 2026-09-20 の修正（v64・「予想のみ」の誤カウントを修正）

**症状**：参加者リストで「参加のしかた」を**「予想のみ」に選び直したあと**、その方の
「原資の対象」（賞金原資を負担するかのチェック）だけを個別に付け直すと、「組み合わせを決める」
チェックリストの残り人数が減らない（＝もう予想のみを選んでいるのに、まだ組が要る人として
数えられ続ける）不具合。

**原因**：「回る人（＝組・枠が必要な人）」の判定 (`plays`) が、参加のしかた（`joinOf()`。
`vote`・`f`という2つのフィールドから毎回“推定”していた）に加えて、`fee`（原資の対象）も
「trueなら回る人」という OR 条件に入れていた。この`fee`条件は、**新規に追加したばかりで
まだ「参加のしかた」を一度も選んでいない参加者**（既定値：`vote:true, f:0, fee:true`）を
正しく「回る人」に数えるために必要（`joinOf()`だけでは f=0 のとき「予想のみ」と誤読するため）。
しかし同じ条件が、**すでに「予想のみ」を明示的に選んだあと**に原資の対象だけを個別に
付け直した人も「回る人」に巻き込んでしまっていた。「未選択の既定値」と「明示的に予想のみを
選んだ後の再設定」を、`fee`という1つのフラグだけでは区別できなかったのが根本原因。

**対処**：参加のしかたのプルダウンで選んだ値そのものを `p.join`（新フィールド）に保存するように
した（`joinSet()`）。`joinOf()` はこの `p.join` があれば最優先で使い、無い場合だけ従来どおり
`f`・`vote` から推定する（＝まだ一度もプルダウンを触っていない新規参加者の既定表示は変えない）。
「回る人」の判定は新しい `needsGroup(p)` にまとめ、`p.join` が明示的に付いている人は
**`fee`を見ずに**その選択をそのまま信じる。付いていない人（未選択の既定値）だけ、これまでどおり
`fee`込みで推定する。新規参加者 (`pAdd()`・貼り付け取り込み) には最初から `join:"both"` を
付けるようにし、曖昧な既定値に頼る場面自体も減らした。

**確認した非対象（壊していないこと）**：新規に参加者を追加した直後（まだ参加のしかたを
一度も選んでいない状態）は、これまでどおり「組み合わせを決める」の残り人数に正しく数えられる
ことをテストで確認済み（`tests/v2test.js`に既存の回帰テストがあり、そのまま緑のまま）。

影響を受けたファイル：`docs/pc.html`（`joinOf`・`joinSet`・`needsGroup`の追加、`pAdd`・
`bulkApply`に`join:"both"`を追加）、`docs/sw.js`（VER: v63→v64）、`docs/README.md`・
`CONTRIBUTING.md`・`README.md`（バージョン表記・検証スイートの項目数を同期）、
`tests/v2test.js`（回帰テストを1件追加）。テストは26本・2274項目、NG 0（前回2273項目から+1）。

---

## 0-3. 2026-09-20 の追加（v63・馬券に「馬連」を追加）

claude.ai 側の設計書「競技定義プラグイン化_設計書」の多競技化の検討（§2.3）で、
既存3券種（枠連・GTO・罰金）に単勝・複勝を加える案を比較検討した結果、
**単勝・複勝は見送り、代わりに馬連（個人名2名の組合せを当てる券種）を実装**した回。

**見送った理由（使い方画面にも明記）：**
- 単勝（1位だけを当てる）→ すでにある「優勝」の賞と当てる対象が重なる
- 複勝（上位何名かに入れば的中）→ 的中しやすいぶん配当が薄くなり、当てる楽しみが小さい
- 馬連 → 16名なら C(16,2)＝120通り。既存の枠連（8枠なら C(8,2)＝28通り）より当てにくく、
  GTOの3連複（C(16,3)＝560通り）ほど当てにくすぎもしない、ちょうど良い難度と判断

**実装は「フル実装」**（枠連と同じレベルまで作り込み）。大会設定の「券種」で
枠連／馬連を切り替える（`DB.meta.kMode`、既定は枠連＝既存動作を変えない）。

| 画面 | 変更点 |
|---|---|
| 大会設定 | 券種の切替セレクトを追加。馬連を選ぶと「枠の作り方」欄が隠れる |
| 予想入力 | 枠のマス目選びの代わりに、GTOと同じ「氏名ボタン一覧」から2名を選ぶ。人気は「口数の多い組合せ順」の一覧（`kPairVotes()`） |
| 予想入力（貼り付け） | `parseKeibaPair()`。「氏名・買い目2名・口数」、氏名の順不同に対応 |
| 結果入力 | 見出しが「当選ペア」になり、枠の選択欄が消える |
| 集計 | 枠のマス目表の代わりに「組合せの人気」一覧。無効口の表も氏名表示に |
| 発表演出 | 枠番のスロット（`drawFrames`）の代わりに、氏名が回って止まる`drawPair()`。氏名表示用に`.big-no.txt`（横長の箱）を追加 |
| 使い方 | 「馬券（馬連予想）」の説明、貼り付け書式、無効になる理由が馬連用に変わる。単勝・複勝を作っていない理由も明記 |

的中判定は「ネット上位2名の氏名の組合せ（順不同）」＝ `DB.result.top2` を直接比較するだけで済み、
既存の枠連の的中判定（`DB.result.frame`）には触れない設計にした。無効判定（`kVoid`）もGTOの
`gVoid` と同じ考え方（出走取消・重複指名を無効化）を流用。

新規テスト `tests/uma.js` を追加（設定切替・無効判定・的中判定・画面・貼り付け・発表演出・
全画面描画の安全性）。テストは26本・2273項目、NG 0（前回2216項目から+57）。

影響を受けたファイル：`docs/pc.html`（本体）、`docs/sw.js`（VER: v62→v63）、`docs/README.md`・
`CONTRIBUTING.md`・`README.md`（バージョン表記・検証スイートの本数を同期）、`tests/uma.js`（新規）、
`tests/help.js`（単勝・複勝を作っていない理由のテストを追加）。

---

## 0-4. 2026-09-20 の追加（v62・追加ルール7種＋よくある賞プリセット）

claude.ai 側の設計書「競技定義プラグイン化_設計書」で検討していた、多競技化（ゴルフ以外への一般化）は
いったん保留にし、**ゴルフ専用のまま**「追加ルール」候補を再検討・実装した回。テストは25本・2216項目、NG 0
（前回2184項目から+32）。

**最大の発見：新しい賞のほとんどは、コードを足さなくても「賞金・順位賞」の中の汎用エンジン
（`PZ().rank[]`・`kindOpt()`・`rankWinner()`）が既にできることだった。** ブービー賞・ブービーメーカー賞・
猛打賞・ベストドレッサー賞・珍プレー賞は、既存の「下から数えて」「手で指定」を選ぶだけで作れるのに、
**その決め方（「下から数えて2＝ブービー」等）を知らないと作れず、埋もれていた**。ここは新しく作るのではなく
「知らなくても作れるようにする」対処（ワンクリック追加ボタン）にした。

| # | 追加した賞 | 実装の中身 | 種別 |
|---|---|---|---|
| 1 | よくある賞プリセット | 「よくある賞をまとめて追加」ボタン（`pzPreset()`）。猛打賞・ブービー賞・ブービーメーカー賞・ベストドレッサー賞・珍プレー賞の5件を、既存の順位賞エンジンの設定値として一括追加。同名の賞が既にあれば増やさない | 既存機能の導線改善（コード追加なし） |
| 2 | 大波賞・小波賞・おしどり賞 | 前半（OUT）・後半（IN）のスコア差から自動計算する `waveOf()` を追加し、`kindOpt`（決め方の選択肢）に `bigWave`（差が最大）・`smallWave`（差が最小）・`pair`（差がゼロ）を追加。18ホールの内訳が無い人は対象外（`null`） | 新規（小） |
| 3 | 早出賞 | 個人ではなく「組」のスタート時刻で決まる賞。`startOf()` を追加し、`kindOpt` に `earliest` を追加。組の時刻が未設定なら対象外 | 新規（小） |
| 4 | イーグル賞・バーディー賞 | ニアピン・ドラコンと違い対象ホールを決め打ちできないため、`birdieRows()` でホール別グロスとパーを毎回自動比較して検出。単価（イーグル／バーディー）を「賞金」画面で編集でき、「順位・表彰」に専用の一覧カードを追加。既定はオフ（`use.birdie:false`）で、既存の大会には影響しない | 新規（中）。トグルが必要な唯一の追加 |
| 5 | とび賞（飛び賞） | 検討の結果、既存の「○位ごとに追加」ボタン（`pzSkip()`）がそのままの実装と判明。新規実装はなし | 既存機能そのもの |

上記のほか、10種類の「利用者ペルソナ」（幹事の立場別のニーズ）と、対戦カード型ミニゲーム（オリンピック・
ラスベガス等）や大会形式そのものを変えるルール（スクランブル・ビンゴ）は設計書側で保留にしてある。
「ニギリ（現金の掛け金）」は違法性の指摘があるため対象外とした。詳細は claude.ai プロジェクト
「CompeMasterPro」の設計書ドキュメントを参照。

影響を受けたファイル：`docs/pc.html`（本体）、`docs/sw.js`（VER: v61→v62）、`docs/README.md`・`CONTRIBUTING.md`
（バージョン表記を同期）、`tests/v2test.js`（新規テスト追加・既存の「追加ルール12枚」への数合わせ）。

---

## 0-5. 2026-09-20 の修正（v61）

「スコア入力」画面の貼り付け欄について、**全員分をまとめて入力できることが伝わらない**という指摘。
見出しを「集計表を貼り付けて取り込む」→「集計表を貼り付けて**全員分**を取り込む」に、
説明文にも「**◯名分すべて**のグロス・HDCP・ネットを…一度に入れます」と人数を明示した
（使い方画面の案内文もこの見出しに合わせて更新。テスト25本・2184項目、NG 0）。

---

## 0-6. 2026-09-20 の修正（v60）

v59で「直した」つもりだった印刷の不具合が、実機の印刷プレビューでは直っていないという指摘を受け、
Playwright（ヘッドレスChromium）で実際にPDFを出して調べ直しました。あわせて、印刷ボタンの表記・配置が
画面ごとにバラバラという指摘も直しました。テストは25本・2184項目すべて合格（NG 0。今回の再発防止テストを5件追加）。

| # | 内容 | 詳細 |
|---|---|---|
| 1 | 印刷が白紙になる不具合（v59の対処では不十分） | v59では「`#work` を `display:grid → block` に」「`height:100%` の連鎖を切る」の2つを試したが、**どちらも効いていなかった**（Playwrightで印刷用PDFを実際に出して確認）。真因は、順位表のカード（`.r-main`）が見出しの直後に収まりきらないと、`.card{page-break-inside:avoid}` の指定によりカードごと2枚目へ送られ、1枚目が見出しだけの空白になっていたこと。集金表（`#sec-collect`）で2026-09-04以前に直したのと同じ原因だったので、同じ直し方（そのカードだけページをまたいでよいことにする。行は`tr`側の`break-inside:avoid`で分けない）を適用。PDF出力で1枚目に本文が出ることを確認済み |
| 2 | 「印刷と発表」カードが画面を移すだけだった | 結果共有・出力画面の「印刷と発表」カードは、押しても画面が変わるだけで印刷が始まらず、紛らわしかった。押すとその画面を開いて**続けて印刷ダイアログまで出す**ようにし、印刷ではない「発表の画面へ」は別カードに分けた |
| 3 | 印刷ボタンの表記が画面ごとにバラバラ | 「印刷」とだけ書かれたボタンが2箇所（集金・精算／賞金・収支）にあり、他画面（順位・表彰＝「競技結果を印刷」、スタート表＝「スタート表を印刷」）と表記が揃っていなかった。すべて「〇〇を印刷」の形に統一（集金・精算→「集金表を印刷」、賞金・収支→「賞金・収支を印刷」） |

**印刷ボタンの配置は現状維持で確定（2026-09-20）。** 上：順位・表彰／スタート表（ツールバー型）。
下：集金・精算／賞金・収支（単票の集計型）。画面の作りに合わせた配置のままとし、表記のみ統一済み（上記3）。
今後、新しく印刷ボタンを足す画面ができたときは、この2パターン（ツールバー型は上、集計型は下）に合わせること。

---

## 0-7. 2026-09-20 の修正（v59。上記v60により印刷の対処は置き換え済み）

利用者から挙がった5件の不具合を直しました。

| # | 不具合 | 直したこと |
|---|---|---|
| 1 | スコアの手入力時に画面が飛ぶ | `render()` が入力のたびに画面を作り直し、表の横スクロール・縦スクロール位置が毎回0に戻っていた。作り直す前後で位置を覚えて戻すようにした |
| 2 | 印刷でヘッダーのあとが白紙ページになる | ~~PC幅（1024px以上）で `#work` を `display:grid` にしているため～通常のブロック表示に戻した~~ **→ 実際には直っておらず、v60で真因を直した（上記参照）** |
| 3 | 集計表の貼り付け読み込みが分かりにくい | 「読み取る」を押しても結果が画面の離れた場所に出て気づきにくかった。読み取り後に結果までスクロールするようにした |
| 4 | 予想のみ（馬券・GTOだけ）参加者のUIが邪魔 | 参加者タブで、回らない（予想のみ）と決めた方の組・枠・実力の欄を、確定済みとして「—」表示にした（車・原資は回るかどうかと無関係なのでそのまま）。「プレーをしない表明＝確定」を画面にも反映 |
| 5 | 発表が終わってもホームの達成度%が変わらない | 「発表」段階の2項目（順位を確かめる・発表する）は繰り返し行える操作で完了状態を持たず、達成度%が常に0%のまま止まって見えていた。発表の段階では%を出さず、文章の案内に変えた |

---

## 1. いま動いているもの

パソコン用の**Webアプリ1本**。HTMLファイル1つで完結し、インストールも通信も不要です。

| ファイル | 役割 |
|---|---|
| `pc.html`（約658KB。上限700KB） | 参加者・組み合わせ・スコア集計・予想ゲーム・賞金・収支・表彰式の演出 |
| `index.html`（1KB） | `pc.html` への転送ページ。トップのURLが404にならないためのもの |
| `sw.js` / `pc.webmanifest` / アイコン4種 / `README.md` | 配布とオフライン動作の部品 |

**配布物は9ファイル**です。

### 正本の場所

```
https://github.com/Takumihiramatsu/CompeMasterPro   →   docs/
```

ナレッジにも `pc.html` `index.html` `sw.js` のコピーを置いています。
**9月4日にリポジトリを一度非公開にした際、Pagesが止まって手元が唯一の正本になった**ためです。
どちらを使うか迷ったら、`sw.js` の `VER` が大きいほうが新しいです。**現在は `compe-v61`。**

```
https://raw.githubusercontent.com/Takumihiramatsu/CompeMasterPro/main/docs/pc.html
```

### 公開ページ

```
https://takumihiramatsu.github.io/CompeMasterPro/          → pc.html へ転送
https://takumihiramatsu.github.io/CompeMasterPro/pc.html   運営システム
```

**非公開にするとPagesは止まります**（無料プランでは非公開リポジトリから配信できません）。
9月3日と9月4日に実際に404になりました。

### 変更したときの手順

0. **`git clone` で最新を取る。**手元のコピーから始めてはいけません（9月8日に枝分かれが発生）
1. 変更する
2. **検証スイートを変更前後で走らせる**（25本・2179項目・NGが0であること）
3. **公開する前に `privacy.js` を通す**（実名の混入を機械で検査）
4. `docs/` を上書き。ファイルを減らしたときは、消えたファイルを個別に削除
5. **`sw.js` の `const VER = "compe-vN"` を必ず上げる**
6. コミット。1〜2分でPagesが再構築される
7. **スマホはホーム画面のアイコンを削除して追加し直す**（CSSの変更は `VER` を上げても残ることがある）

---

## 2. 運営システムの中身

16の画面（v47から。4.10・4.11参照）。使わない機能は「追加ルール」で外すと、画面ごと消えます。
PCは左のサイドバー、スマホは下タブと大会ハブの「すべての画面」から選びます。**起動するとホームが開きます。**

画面の一覧の外に、入口の画面が2つあります（サイドバーも上の帯も出さない1枚の画面）。

| 入口 | できること |
|---|---|
| ホーム | いまの大会（進み具合・「つづきから」）・新しい大会・前回のファイルからコピー・保存・最近のファイル |
| 作成画面 | 基本 → 競技方法 → 追加ルールの3段。**参加者が0名のときだけ**開ける |

| 画面 | できること |
|---|---|
| 大会ハブ | 次にやること・数字のカード3枚・いまの段階のチェックリスト・使っている追加ルール・次の段階の流れ・集金の見込み |
| 大会設定 | 基本4項目／使っている追加ルールの札／詳しい設定（コース・競技方法・技能賞のホール・予想ゲーム・お金） |
| 追加ルール | 11個のルールの切替（競技・予想ゲーム・賞とお金）。技能賞はパーと合わなければその場で直せる |
| 参加者 | 表は「基本（氏名・所属・生年月日・組）」「枠と実力」「参加とお金」を切り替える。**Excelから貼り付けで一括登録**（PCでは右の列） |
| 組み合わせ | 組のカードと未割り当てのトレイ。ドラッグ（PC）／押して行き先を選ぶ・長押しで運ぶ（スマホ）。最初の時刻＋間隔で全組のスタート時刻。スタート表を印刷 |
| 予想入力 | 馬券・GTOを上で切り替え。**枠のボタンを2つ押す／人を順に3名押す**。貼り付けで一括取り込み、同じ買い目は口数でまとめる。右に人気のマス目と売上。**締め切ると入力が止まる**（解除あり） |
| 賞金・収支 | 順位賞・技能賞・団体賞・抽選賞／収支 |
| 集金 | 1人ずつの徴収額と受け取りチェック。スマホでは1人1カード |
| スコア入力 | ペリア方式の集計。**打ち込みの検算**と**貼り付け取り込み**。3方式（上で切替）。**Enter で次の欄**。集計表方式は右の列に検算・貼り付け。**ハンディ／グロスの直接入力**と**棄権・ノーカードの印** |
| ニアピン・ドラコン | 何でもニアピンを含む3種。**対象ホールがパーと噛み合わないと警告** |
| 順位・表彰 | 競技結果（上位3名を大きく・**賞の列**）・同点の理由。右に技能賞・発表の順番・賞金合計。馬券・GTO・罰金へ反映 |
| 集計 | 人気マス目（当選に印）・得票数・1人ずつの支払額・**無効になった口の一覧** |
| 結果・発表 | 結果の入力と表彰式の演出（1人を発表する画面は2列・上下に枠と次の予告）。**操作パネルを別ウィンドウで開ける** |
| 結果共有・出力 | ファイルに書き出す・結果の文章（コピー）・印刷する画面への案内 |
| データ | 自動保存・ファイル保存・CSV |
| 使い方 | 手順とルールの説明。**設定に応じて文面が変わる** |

### 主要な計算

```
ハンディ（ダブルペリア）＝（隠し12ホールの合計 × 1.5 − コースパー）× 0.8
ハンディ（新ペリア）    ＝（隠し6ホールの合計  × 3   − コースパー）× 0.8
ネット ＝ グロス − ハンディ
配当金 ＝ 売上金 ÷ 当選口数（100円未満切り捨て）／残金は経費へ
```

1ホールの上限（トリプルパー等）とハンディ上限は設定で切り替え可能。同スコアは年長／年少／ハンディ小から選べます。

### 内蔵データ（2026-09-08に全国へ拡張）

**全47都道府県 2,254施設・5,122コース**を9ホール単位で埋め込み済み。
都道府県と名前で検索でき、前半9H・後半9Hを選ぶとパー配分が入ります。

圧縮した形（`MSTZ`）で持ち、読み込み時に一度だけ `MASTER` へ展開します。
パーを配列でなく9桁の文字列に、都道府県を番号にすることで **317KB → 200KB** に収めました。`pc.html` は398KBです。

**検索用に正規化した名前を展開時に作っています。** これが無いと、検索欄に1文字打つたびに
2,254件ぶんの正規化をやり直すことになり、8ms以上かかっていました。いまは0.1msです。

**パー6・パー7のホールが6コースあります**（出典どおり）。トリプルパーカットの上限が
変わるので、該当コースを使うときは注意してください。計算自体は `パー×3` で
自動的に正しくなります。

収集の手順・欠番の扱い・収録できなかった32ページなどは、ナレッジの
「ゴルフ場マスタ 全国版 引き継ぎメモ」に詳しく書いてあります。
残る課題は、会員制コースの取りこぼし補完（愛知以外の46都道府県が未突合）です。

---

## 3. スコア入力の考え方（9月4日に確立）

### 正規手順：打ち込んで1行ずつ検算する

「スコア入力」の画面を**「ゴルフ場の集計表から」**にして、
集計表の**グロス・HDCP・ネットの3つ**を入れます。
アプリが `グロス − HDCP ＝ ネット` を行ごとに照合し、○か×を出します。

**3つのうち1つでも打ち間違えると崩れるので、全行が○になればそのデータは正しいと言い切れます。**
16名で3〜4分。これは読み取りでは絶対に得られない保証です。

- 入れたネット（`netRef`）は**順位の計算には使いません**。順位は常に `グロス − HDCP`
- 差の大きさも表示されるので、どこが違うか見当がつく
- 2つしか入っていない行（グロスとネットだけ等）は、計算で補った値と照合しても必ず一致してしまうため、**照合の対象から外します**

### 貼り付けも使える。読み方は3通りを自動で試す

| | 方法 | 使う場面 |
|---|---|---|
| ① | 行ごとに読む | **PDF**・Excel・手打ち |
| ② | 改行を無視して数字の並びから組み直す | 改行が崩れた |
| ③ | 表が縦向きに読まれたものとして列で読む | 写真から読み取った文字 |

②は次の4つが同時に成り立つ23個のかたまりを拾います。偶然そろうことはまず無く、見出し行やPAR行は自然に外れます。

```
前半9の合計 ＝ OUT ／ 後半9の合計 ＝ IN ／ OUT＋IN ＝ Total ／ Total−HDCP ＝ Net
```

**注意点：数語ごとに改行が入ると、①は「16行取れたつもり」で誤った値を返します。**
取れた数だけで判断すると切り替わらないので、**同数でも②③を採る**作りにしてあります。

**氏名は省略できます。** 数字だけを上から順に貼れば名簿の並びに当てます。
ただし並びを間違えると全員のスコアが入れ替わるため、当てた旨を画面で強く知らせ、
**行数が名簿とそろわないときは何もしません**。

### PDFについて（2026-09-18に宿題を取り下げ）

「ゴルフ場に集計表のPDFを頼む」宿題は**取り下げました**（平松さんの判断。文字認識を検討していた頃の名残で、打ち込みの検算で足りる。4.22）。
手元にPDFやExcelの文字があれば貼り付けでも入ります（試したときは16名・18ホール分が取り込めた）が、**頼んでまで用意するものではありません。**

---

## 4. 読み取りアプリを廃止した理由（2026-09-04）

**来年また同じ検討を始めないために、経緯を残します。**

スマホで集計表を撮り、端末の文字認識で読んでPCへ渡す、という専用アプリを作っていました。
実機で試した結果、**23列の密な集計表には実用にならない**と判断して廃止しました。

### 実際に起きたこと

1. **表が縦向き（列ごと）に読まれた。** 1行＝表の1列（Net・Hdcp・Total・OUT・IN・各ホール）となり、1行の中に16名ぶんの値が並ぶ
2. **ホール別の数字は取りこぼしと誤読が多い。** 空欄が多数、`∞` などの誤読が混じり復元できない
3. **改行が正しく返らない。** 繋がる／数語ごとに切れる／行の途中で切れる、すべて発生
4. **iPhoneの「テキストをスキャン」がメニューに出ない**（ホーム画面から起動したアプリで）

1〜3は解析側で対応しましたが、**撮影条件が変わるたびに出力の形が変わり、追いかけても切りがない**という結論に達しました。

### 検討して却下した案

| 案 | 却下の理由 |
|---|---|
| Tesseract.js等のOCRを積む | 5〜8MBになり「単一HTMLをUSBで持ち運ぶ」設計が壊れる。精度もApple Live Textより低い |
| 四隅をタップ→台形補正→格子に分割→1文字ずつ判定 | 技術的には正しいが368マスあり、1マスでも誤れば手直しが必要。手入力より遅くなる |
| クラウドのOCR APIへ送る | オフライン要件に反し、個人情報を外部へ出すことになる |
| PDF読み込み機能をアプリに積む | PDFの日本語は符号化されており氏名の取り出しが不確実。「開いてコピーして貼る」で足りる |

### 撤去した範囲

- `index.html`（読み取りアプリ）→ `pc.html` への転送ページに置き換え
- `manifest.webmanifest`・`scan-*.png` 4点 → 削除
- PC側の `scanLoad` / `scanFill` / 「読み取りファイルを取り込む」ボタン → 撤去
- テスト `scan.js`（112項目）・`handoff.js`（30項目）→ 廃止

**貼り付け解析（①②③）はPC側に残してあります。** 写真経由も動きますが、
「うまくいったら儲けもの」の位置づけです。使い方タブにもそう書いてあります。

---

## 4.5 9月8日にやったこと

### ゴルフ場マスタを全国版に（2,254施設・5,122コース）

中部5県379施設から約6倍。圧縮形（`MSTZ`）で持ち、読み込み時に一度だけ展開します。
`pc.html` は402KBです。詳しくは「2. 内蔵データ」を参照。

### サンプルの大会を本番規模に

| | 前 | 後 |
|---|---|---|
| 組・枠 | 4組4枠 | **8組8枠** |
| 馬券 | 32口 | **120口・24,000円** |
| GTO | 32口 | **52口・26,000円** |
| 技能賞 | 一部空欄 | **全ホール埋めた** |
| ネット・グロス | 同着あり | **16名すべて別** |

当選枠 `1-2`（的中6口・4,000円／口）、GTO 3連複で的中13口、罰金4,000円まで確定した状態です。
**当日の予行演習がそのままできます。**

作る途中で4つつまずきました。いずれも `v2test.js` に検査として残してあります。

1. **8組を作っても4組に戻る** — `gAuto()` が `groupSize`（既定4）で割り直す。先に `groupSize=2` を設定する
2. **当選枠が `1-1` になる** — 枠は組の順に振られるので、スコア順に並べると上位2名が同じ枠に入る
3. **GTOが全額返金になる** — 選抜者を下位3名からずらすと、的中する口が一つも無くなる
4. **罰金が0円になる** — 選抜者と下位3名が完全一致すると、負ける人がいなくなる。**意図的にずらす**

### 組と枠は別のもの（2026-09-08に整理）

**混同しやすいので、ここは必ず読んでください。**

| | 意味 | 16名なら |
|---|---|---|
| **組** | 同じ時間に一緒に回る単位 | **4組 × 4名** |
| **枠** | 馬券の賭け単位 | **8枠 × 2名** |

枠づくりで大事なのは2つです。

1. **組をまたぐこと。** 同じ組の2人で枠を作ると、一緒に回った者同士になり、
   ネット上位2名が同じ枠に入りやすくなって馬券が荒れます
2. **実力が拮抗すること。** 強い2人の枠ができると、そこに票が集まってゲームになりません。
   上手い人と苦手な人を組ませて、どの枠も似た強さにします

そのため参加者タブに**「実力の目安」**（前年のスコアなど）の欄を足しました。
順位や賞金の計算には一切使いません。枠を割り振るときだけ使います。

「枠を2人ずつ割り振る（組をまたぐ）」を押すと、目安が入っていれば
**いちばん上手い人といちばん苦手な人から順に突き合わせ**ます。
目安が無ければ、組をまたぐことだけを守って割り振ります。

参加者タブの下に**枠の出来**が出ます。同じ組どうしの枠がいくつあるか、
実力の合計がいちばん強い枠と弱い枠で何点開いているか。差が小さいほど票が散ります。

**あわせて不具合を1つ直しました。** 枠を自分で決める運用のとき、
組を「未定」にしても枠が残り、**出走者に数えられたままでした**。
組がなければプレーしないので、枠も外すようにしました。

### サンプルを2種にした

「データ」タブに2つのボタンがあります。

| | 中身 |
|---|---|
| **① ふつうの大会** | 16名・**4組4名／8枠2名（組をまたぎ実力拮抗）**／馬券100口・GTO50口／技能賞8ホール／18ホール分のスコア。順位も配当も罰金も出た状態。**当日の予行演習用** |
| **② つまずきやすい大会** | 同ネット・同グロス・**得票が並ぶ選抜者**・欠席者2名・ノーカード1名・**無効になる口3種**・該当者のいない技能賞・パーと噛み合わない対象ホール・**同じ組どうしの枠**。**迷ったときにどう出るかを事前に見るため** |

②は当日いちばん困る「決まりきらない場面」を一度に起こします。
アプリが勝手に決めたように見える場面（同ネットは年長者、同グロスは下位3名の並び、
得票が並んだら手動指定）を、本番前に目で確かめておくためのものです。

### 当日いくら集めるかを事前に出せるようにした

**当日の朝に集金するので、前の晩までに「ひとり◯◯円」と言える必要があります。**
スコアも投票もまだ無い段階で金額が出るようにしました。

```
プレー代（グリーンフィー・昼食）   13,500円
パーティ・飲み物代                2,000円
会費（賞金の原資）                6,000円
馬券（見込み 6口）                1,200円
GTO（見込み 3口）                1,500円
────────────────────────────
ひとりあたり                     24,200円
16名ぶん 合計                   387,200円
```

**プレー代は幹事が預かってゴルフ場へ払うぶん**です。賞金の原資（会費）とは別に数えます。

項目には**数え方が2つ**あります。宿泊ありのコンペではここを取り違えると
集めすぎ・足りないが起きます。

| 数え方 | 使う場面 | 例 |
|---|---|---|
| **ひとり** | 全員が同じ額を払うもの | プレー代・昼食代・宿泊代・夕食代 |
| **1台あたり** | 単価 × 台数を集め、**配車した担当へ返す** | 車代・ガソリン代・高速代 |
| **総額を頭割り** | 総額が先に決まっているもの | 貸切バス代 |

**単価と、車を出す方の選択。どちらかが空だと0円になります。**
大会設定の**「車を出す方と台数」**で、出す方にチェックを入れてください
（参加者タブでも同じ欄が編集できます）。

**1人1台で数えます。** 1人が2台出す場面は考えにくいので、台数ではなくチェックだけにしました。
前の版で2台と入れたデータを開いても1台として数えるので、集めた額と返す額はずれません。

2026-09-08、ここで2回つまずきました。
1回目は台数を入れ忘れて「収支に反映されない」。
2回目は**台数の欄を出す条件を「金額が入っていること」にしていた**ため、
項目を足した直後は金額が0で欄そのものが現れず「台数を入力する方法がない」。
いまは**「1台あたり」を選んだ時点で欄が出ます。** 金額と台数のどちらが空かも別々に知らせます。

**車の代金は幹事が一度預かるだけです。**「1台あたりの単価 × 台数」を全員から集め、
そのまま配車した担当へ返します。台数は参加者タブの「出す車」欄に入れてください。
集めた額と返す額は必ず一致します（`fee.js` が毎回確かめます）。

収支では**「配車した担当へ返す」と「ゴルフ場への支払い」を分けて**出します。

頭割りは**割り切れないときは切り上げ**ます。足りないと幹事の持ち出しになるためです。
余りは画面に出るので、経費に回してください。

よくある項目はボタン1つで足せます（プレー代・昼食代・パーティ代・宿泊代・夕食代・
車代・ガソリン代・高速代）。日帰りと宿泊ありのどちらにも対応しています。

予想ゲームは実際に何口買うか分からないので、**見込みの口数**で見積もります。
投票が入ると自動で実際の口数に切り替わります。

収支では**収入に「プレー代の預かり」、支出に「ゴルフ場への支払い」を同額**で立てて
相殺します。片方だけに入れると黒字赤字がその分だけ狂うためです。

ひとりずつの徴収額にもプレー代が入るので、**アプリの数字がそのまま集金額**になります。

### 「会費」ではなく「賞金の原資」

**「会費」だと当日集める総額と取り違えられます。** 実際は賞金を出すためのお金で、
プレー代とは別物です。アプリ全体で**「賞金の原資」**に言い換えました。

大会設定では、上から下へたどると金額が妥当か分かる形にしてあります。

```
賞金の原資
  ひとりから集める額  6,000円  × 対象16名 ＝ 96,000円

この原資から出す賞金
  順位賞   24,000円（優勝10,000 / 2位5,000 / …）
  技能賞    8,000円（ニアピン4ホール × 1,000 / …）
  団体賞    5,000円
  抽選賞    2,000円
  ────────────────
  賞金の合計 39,000円
  原資の残り 57,000円  ← 経費や次回への繰越

  ひとり6,000円 集めて、賞金として 2,438円 ぶんが戻る計算
```

**賞金が原資を上回ると赤く警告**し、「集める額を上げるか賞金を減らす」と案内します。

### 賞は名前で発表する（2026-09-08）

以前は優勝・2位・3位・ベスグロ以外をまとめて**「その他の賞」**として1枚で出していました。
**受け手には何の賞か分かりません。**

いまは**見出しをその賞の名前にして1枚ずつ**出します。

```
ベ ス ト グ ロ ス → 第3位 → 第2位 → 優勝 → 団体賞
  → 5 位 → 1 0 位 → B B
```

賞が6つ以上あるときは、**5枚目までを1枚ずつ、残りをまとめて1枚**にします。
まとめた画面でも見出しに賞名が並びます（「7位　・　8位　・　9位」）。
飛び賞を10個作っても表彰式が間延びしません。

### 当日モード（2026-09-08）

ヘッダー右上の切替で、**準備中**と**当日モード**を行き来します。

当日モードにすると、準備のタブ（大会設定・参加者・組み合わせ・予想入力）が隠れ、
**11タブが7タブ**になります。画面のいちばん上には当日やることが4つ並びます。

```
当日やること ── この4つだけです
  １ 集金する          ２ スコアを入れる
  ３ ニアピン・ドラコン   ４ 発表する
```

**機能は消していません。** 当日は準備のタブを触らないし、触ると壊しかねないので隠すだけです。
戻すときはもう一度切替を押します。隠していることと戻し方も画面に出ます。

使わない機能を外していても、手順の番号は1から順に振り直されます。
開いているタブが隠れる場合は、見えるタブへ自動で移ります。

**UI/UX評価（2026-09-08）で最優先とした改善です。** 実測で大会設定だけ9,875文字、
全体で24,193文字あり、年に数回しか使わない幹事には量が多すぎました。
残る改善案は「大会設定を畳む」「集金表をスマホで縦持ちに」。

### 徴収額の丸めと繰越金

当日の集金でお釣りを出さないよう、**ひとりの徴収額を500円か1,000円の単位に切り上げ**られます
（大会設定の「徴収額の丸め」。既定は丸めない）。

```
ひとり 43,601円  →  500円単位なら 44,000円（+399円）
16名ぶんの切り上げ 4,585円  →  繰越金へ
```

**切り上げたぶんは幹事の取り分ではありません。** 収支の収入に
「端数の繰越（500円単位に切り上げ）」として立ち、次回へのプールになります。
集金表にも「切り上げ」の列が出て、ひとりずついくら上乗せしたかが分かります。

ちょうどの額なら切り上げません（5,000円は5,000円のまま）。

### 一律と個別を分けて数える

**実費と会費は全員同じ額（一律）。馬券とGTOは人によって違う（個別）。**
この2つを混ぜて「ひとりあたり◯◯円」と言うと、当日かならず食い違います。

```
一律　全員が同じ額（プレーする15名）
  プレー代・宿泊代・夕食代・車代・ガソリン代    33,301円
  会費                                      6,000円
  ────────────────────────────────
  一律の小計          39,301円 × 15名 =   589,515円

個別　人によって違う（投票する16名）
  馬券 102口 × 200円                       20,400円
  GTO   28口 × 500円                       14,000円
  ────────────────────────────────
  個別の小計                                34,400円

徴収合計                                  623,915円
```

**当日の朝に集めるのは「一律」まで。** 馬券とGTOは投票を締め切ってから足します。

一人あたりの金額が決まる時点は3つあります。

| 時点 | 分かること |
|---|---|
| 大会設定を入れた直後 | だいたいいくら（見込みの口数で見積もる） |
| **投票を締め切ったとき** | **集める額が確定**。ここで集金できる |
| プレー後 | 返す額が確定。過不足を精算 |

**頭割りの分母が動くと全員の額が変わります。** 当日朝の欠席や相乗りの変更がそれです。
16名で集めたあと1名欠けると、ガソリン代は 2,313円 → 2,467円 に上がります。
集金したあとに前提が変わったときの差額管理は、v55で作りました（4.19）。

集金表は「一律／個別／精算」の3区分で並びます。

### 参加のしかたは3択

参加者タブの**「参加のしかた」**で選びます。

| 選択 | 意味 |
|---|---|
| **プレー＋予想** | ふつう。回って、馬券・GTOも買う |
| **プレーのみ** | 回るけれど、予想ゲームには参加しない |
| **予想のみ** | 回らず、馬券・GTOだけ買う |

**「投票」と「投票のみ」の2つのチェックを並べていたのを、1つにまとめました。**
前者は「予想ゲームに参加するか」、後者は「プレーするか」で軸が違い、
並べると区別がつかないという指摘を受けたためです（2026-09-08）。

「予想のみ」を選ぶと、組と枠が外れ、実費と賞金の原資の対象からも抜けます。

| | 扱い |
|---|---|
| 実費（プレー代・宿泊代・車代） | **かからない** |
| 会費 | **かからない** |
| 馬券・GTOの代金 | **集める** |
| 的中の払戻 | **払う** |
| 順位・技能賞・罰金 | **対象外**（プレーしていないため） |

集金表では氏名の横に**「投票のみ」**と出て、表の下に人数と金額が並びます。
当日いちばん間違えやすいので、`fee.js` が収支の全部を固定しています。

### 発表の並びを直した

**馬券の当選枠は「ネット上位2名が入る枠」なので、先に出すと優勝者が割れます。**
2026-09-08まで「馬券 → 順位発表」の順で、「上位2名」の画面で優勝者と2位の氏名が
出たあとに第3位→第2位→優勝と発表していました。答え合わせになっていました。

いまの並びは次のとおりです。

```
技能賞 → 競技結果（ベスグロ・3位・2位・優勝）→ 団体賞 → 賞（5位・10位・BB…）
  → 馬券 → GTO → 選抜者・罰金 → 抽選 → 締め
```

**予想ゲームの中では馬券を先に出します。** 馬券は上位2名（明るい話）、
GTOは下位3名（落ちの話）なので、この順のほうが流れが自然です（2026-09-08）。

**予想ゲームは順位が確定してからの精算**なので、後に置くのが筋です。
GTOは下位3名なので、優勝の発表とは情報が重なりません。

幕番号（第1幕・第2幕…）も**通し番号で自動に振る**ようにしました。
使わない機能を外しても番号が飛びません。

### 発表で空白を出さないようにした

受賞者のいないホール、氏名の入っていない順位を発表すると場が止まります。
**読み上げる相手がいる画面だけ**を出すようにしました。

- 技能賞：受賞者のいないホールは飛ばす。**1ホールも受賞者がいなければ見出しごと出さない**
- GTOの下位3名：氏名が入っていない順位は飛ばす
- 選抜者：1人も決まっていなければ選抜者と罰金の幕ごと出さない
- 馬券の上位2名：氏名が取れていなければ出さない

サンプル②で試すと、34枚が28枚になり、空白の画面は0枚です。

### 表の列が潰れる問題

`width:` は表が幅100%に押し込まれると**縮みます**。`min-width:` なら縮みません。
「賞の名前」「決め方」が1文字まで潰れ、氏名の選択欄も切れていました。
文字の入る26箇所を `min-width` に変え、氏名を選ぶ欄は矢印のぶんを含めて11.5emにしました。

### ホーム画面のアイコンが404になる

**iOSのアイコンは追加した瞬間のURLを覚えます。** サイトが404だった時期に追加すると、
以後ずっとそのURLを開きに行きます。`404.html` を置いて、どのURLで固定されていても
本体へ送り返すようにしました。

**根本の原因は、Pagesの公開元が `/(root)` に戻っていたことでした。**
リポジトリを非公開にして再公開すると、`/docs` の設定が外れて既定値に戻ります。
**非公開化のあとは必ず Settings → Pages を確認してください。**

### 別の作業から取り込んだもの

| 機能 | 内容 |
|---|---|
| `fPair2()` | 参加者タブの「枠を2人ずつ自動で割り振る」。出走なしの人は飛ばし、奇数なら最後の枠だけ1人 |
| `table.hscroll` | 18ホール分のスコア入力表が、`.wrap` の1200pxを超えたときにカードからはみ出す問題の修正 |

---

## 4.6 9月16日にやったこと

### UI/UX を数えて評価した

実測した結果です。**問題は機能ではなく「量」と「順序」**でした。

| | |
|---|---|
| タブ | 11本 |
| 入力欄・選択・ボタン | 443／109／162 |
| 画面の本文 | 全部で 24,193文字（**初めて開いた状態でも 16,271文字**）|
| 大会設定だけで | **9,875文字**（全体の41%）。うちコース設定が 7,381文字 |
| 参加者の表 | 14列・最小でも 1,215px（iPhoneの**3.1倍**）|
| 集金表 | 18列・最小でも 1,172px（**3.0倍**）|

年に数回しか使わない幹事には多すぎます。**ただし削ってはいけません。**
必要だから作られた機能です。見せる順番と初期状態を変えるのが正解です。

優先度をつけた改善案は12件。着手したのは1件目（当日モード）。
**残っているのは次の2つで、どちらも当日の負担に直結します。**

1. **大会設定を「基本」と「詳しく」に畳む**（9,875文字 → 約2,000文字。難易度 中）
2. **集金表をスマホで1人1カードに**（横スクロール3.0倍の解消。難易度 中）

### 別アカウントから届いたデザイン案の判断（2026-09-16）

`CompeMasterPro_再設計_dc.html`（145KB）を受け取りました。**統合しませんでした。**

**動くアプリではありません。** `design_doc_mode content="canvas"` の付いた
画面の絵で、順位計算・馬券・GTO・ペリア・ゴルフ場マスタ・保存・発表が
**1つも入っていません**。置き換えれば全機能が失われます。
外部フォント（Google Fonts）と `./support.js` を読むため、
**オフライン完結の原則にも反します**。

**ただし設計としては質が高く、採るべき点が4つあります。**

| | 内容 | 状態 |
|---|---|---|
| ① | フェーズを3つに束ねる（準備 → 当日 → 発表） | 当日モードは2段階で実装済み。3段階への拡張は未着手 |
| ② | **「次にやること」を常に1つだけ大きく出す** | **未着手。次の一手はこれ** |
| ③ | 追加ルールは後付けで、入れると画面が増えると見せる | 未着手 |
| ④ | 組合せをカードとトレイで（ドラッグ） | 未着手 |

**採らないと決めたもの**（理由つき。同じ提案が来たら読み返すこと）

- **配色の変更（チャコール×ライム）** … 深緑×ブラスは表彰式の格に合う。好みの問題
- **外部フォント** … オフラインが壊れる。**絶対に入れない**
- **大会一覧・過去の大会** … 1大会1ファイルという設計の根幹を変える
- **作成ウィザード** … 年数回なら毎回ウィザードは苦痛。畳んで見せるほうが良い

### 次の一手

→ **同日に着手して段階1まで済ませました。**「4.7」を参照。

---

## 4.7 デザインからの引き継ぎ（2026-09-16）

別アカウントのデザイン作業から、**第2版の再設計案**（`CompeMasterPro-Redesign_dc.html`・
`support.js`・引き継ぎREADME）が届きました。第1版で却下した理由のうち
**配色・外部フォント・1大会＝1ファイルの3点は解消済み**でした（外部フォントの読み込み0件を確認）。
同梱の `pc.html` は GitHub の `docs/pc.html` と1バイトも違わない版でした。

**全面置き換えではなく、3段階に分けて取り込むと決めました。**
デザイン案は静的な絵で、`DB` の構造も現行と違います。**`DB` は変えず、見せ方だけを移します。**

### 段階0：土台を直した

- **`money.js` が83行目で例外になり、以降の検査が一度も走っていなかった。**
  器が `calcK`／`calcG` を返していなかったため。異常終了すると最後の「合計 NG」が出ないので、
  一括実行の一覧で見落とされていた。器を直すと、**9/8の仕様変更に追従していない期待値が2つ**
  見つかった（徴収額にプレー代が入った／「その他の賞」を賞ごとの画面にした）。どちらも実装が正しい
- `privacy.js` の日付の正規表現が「2026-09-16」を「2026-09-1」で切っていた。日の2桁を先に試すように直した
- `mst.js` の容量の上限を 460KB → **480KB**（段階1で約13KB増えたため。理由をテストに記載）
- `VER` を `compe-v46` に

### 段階1：準備 → 当日 → 発表 と「次にやること」

| | 内容 |
|---|---|
| **3段階の切替** | ヘッダー右の「準備中／当日／発表」。前の段階のタブは**1行に畳む**（`✓ 準備 4項目 ▸`）。押せば開く。**消さない** |
| **タブ列** | 段階ごとに見出し（`1 ／ 準備 ・ NOW`）。当日は**集金 → スコア → 技能賞**の順。済んだタブに `✓` |
| **次にやること** | 使い方以外の全画面の最上部。**終わっていない最初の1つだけ**を大きく出し、「開く」で飛ぶ。段階の手順を下に並べる |
| **使う機能の予告** | 大会設定の機能カードに「画面が増える：予想入力・集計」など。足したタブには開くまで `NEW` |

**以前の「当日モード」は「当日」に含まれます。** 畳むタブ（大会設定・参加者・組み合わせ・予想入力）は同じです。
古いファイルの `dayMode` は読み込み時に `phase` へ移し、`dayToggle()` も互換のために残しています。

**「次にやること」の判定は入力済みのデータから毎回出します。ファイルには何も足しません。**

| 段階 | 手順（使う機能だけ出る） | 済んだとみなす条件 |
|---|---|---|
| 準備 | 大会の基本 | 大会名・会場・開催日 |
| | 技能賞の対象ホールを直す | `holeWarn()` が空 |
| | 参加者を登録する | 氏名のある人が1人以上 |
| | 組み合わせを決める | 回る人全員に組（「予想のみ」は数えない） |
| | 馬券の枠を決める | 組のある人全員に枠 |
| | 当日集める額を決める | 実費か原資のどちらかに金額 |
| | 予想を入力する | 馬券かGTOが1口以上 |
| 当日 | 集金する | 徴収額のある全員にチェック。「下の表へ ↓」で集金表まで送る |
| | スコアを入れる | 組のある全員のグロス |
| | ニアピン・ドラコンを入れる | 全ホールに受賞者 |
| 発表 | 発表する | —（常に次の作業として出る） |

**欠場・ノーカード・該当者なしは空欄が正しい**ので、判定が済まないことがあります。
そのため**「『発表』へ進む」を必ず並べ**、判定が段階の切替を妨げないようにしました。

### あわせて直した不具合（どちらも以前からあったもの）

1. **サンプル①で「パーと噛み合わない」警告が出ていた。** 対象ホールが（実在のコース）のもの（ニアピン2・4・11・15／
   ドラコン8・16）なのに、パー配分が既定のままだった。「次にやること」の先頭に出て発覚。
   **対象ホールのほうを既定のパー配分に合わせた**（ニアピン3・7・11・16／ドラコン4・14）。
   実在のコースの配置をサンプルに持ち込まないためでもある。スコアは変えていないので順位・HDCPは不変
2. **ヘッダーの保存表示が、予想入力の空欄の文言（「まだ投票がありません…」）になっていた。**
   一度も自動保存していないときに出る。「自動保存はまだありません」に直した

### 大会と会場の結び付きを消した（案A）

`tests/` の7本と `pc.html` のコメントに、**2026年の大会の会場が実名で**書かれていました。
会場名そのものはマスタに入る公開情報ですが、開催日・実際のスコア・部署名入りの大会名と並ぶと
「どこで誰が何打だったか」が分かります。README の約束（実在の会場名を含まない）にも反していました。

| | 対応 |
|---|---|
| `haiya.js` | 会場名を「（実在のコース）」、大会名の部署名を外す。**マスタから会場を選ぶ検査（1.5、6項目）は外した**（会場名なしでは書けない。一致は9/4に確認済み） |
| `mst.js`・`sel.js`・`ime.js` | マスタ検索の例を、大会と無関係な**信濃ゴルフ倶楽部**（長野県・9H×2・パー72）に差し替え |
| `rules.js`・`sheet.js`・`pc.html` | コメント・読み取りテストの文字列を「（実在のコース）」「○○CC」に。検索欄の入力例は地名（軽井沢）に |
| `privacy.js` 9章（+5項目） | **リポジトリ全体**（tests・文書・pc.html のマスタ以外）で、マスタのゴルフ場名・略称（◯◯CC）・検索語が**理由を書いた語だけ**であること。部署名入りの大会名が無いこと |

`privacy.js` 9章は、**直す前のテストに当てると3項目とも落ちる**ことを確かめてあります。
実名の一覧は持たず、「置いてよい語」を理由つきで並べる作りです（既存の方針と同じ）。
**限界：** 地名だけ（「◯◯」単独）の記述は拾えません。検索語に渡す形（`mQuery('…')`）と略称は拾います。

**git履歴には残っています。** 消すかどうかは「11. 次にやること」の宿題のまま。

### デザイン案と現行の食い違い（対応を決めたもの）

**同じ提案が来たら読み返してください。**

| デザイン案 | 現行の確定事項 | 対応 |
|---|---|---|
| 発表順「ブービー→…→優勝→ベスグロ」。馬券・GTOの発表なし | 9/8確定：技能賞→ベスグロ・3位・2位・優勝→団体賞→各賞→馬券→GTO→罰金→抽選 | **現行の順を維持**。見た目（P11）だけ取り込む |
| 口数の上限＝集金済み口数 | 馬券・GTOの代金は投票を締め切ってから集める | 採らない |
| 「締切まで12分」 | 締切は前夜に会長が決める。時刻を持たない | 「締め切る」ボタンだけにする |
| PCのスコア入力で Enter → 次の欄 | 落とし穴2（変換確定のEnter） | 数値欄だけ、`isComposing` を除外して実装。再現テスト必須 |
| 検算エラーに「集計表が正しい」 | `netRef` は順位に使わない。集計表が正しいなら誤りはグロスかHDCP | 「グロス／HDCPを直す」に変える |
| 競技方法の既定「新ペリア・隠し12H」 | 新ペリアは隠し6H。2026年はダブルペリア | 既定はダブルペリア |
| 大会作成ウィザード | 9/16に却下（毎回は苦痛） | 参加者0名のときだけ出す |
| 最近開いたファイル一覧／100円単位の丸め／GTOは最下位から順に | オフラインでファイル一覧は持てない／丸めは500・1000円／順番は3連単のときだけ | ファイル名の履歴だけ／現行のまま／3連単のときだけ説明 |

### 段階2・3

- **段階2（中）**：→ **済（4.8）**
- **段階3（高）**：組み合わせのカード＋トレイ＋ドラッグ（P6、長押し）／枠の人気表（P10）／
  発表スクリーンの刷新と操作パネルの別ウィンドウ化（P11）／スマホの下タブ・テンキー・表のカード化（M1〜M3。集金表の1人1カードもここ）

1回で段階2まで進めると、テストの修正量が大きくなり、どこで壊れたかが追えなくなります。**段階ごとに区切ってください。**

## 4.8 デザイン案の段階2（2026-09-16）

### 大会設定を「基本」「追加ルール」「詳しく」に分けた（P2・P3）

**大会設定で開いて見える文字が 9,752字 → 820字**（サンプル①・画面に出る文字を実測）。
中身は消していません。畳んだ中を含めると従来どおり約1万字あります。

| まとまり | 中身 |
|---|---|
| **基本** | 大会名・会場・開催日・1組の人数。「この4つで大会が作れます」 |
| **追加ルール** | 11個をカード＋切替スイッチに。「競技／予想ゲーム／賞とお金」の3つに分け、使っている数を出す |
| **詳しい設定**（畳む） | コース／競技方法／技能賞の対象ホール／予想ゲームと罰金／賞金の原資と当日集める額 |

- 畳んだ見出しに**今の設定を1行で**出す（例：`ダブルペリア・上限パー×3・ハンディ上限30・同スコアは年長者`）
- **直す必要があるものは開いた状態で出し「要確認」を付ける**（パーと合わないホール、原資を超える賞金）
- 「次にやること」の「開く」から、畳んだ中へ直接送る（`goStep` が開いてから移る）
- 開閉は**ファイルに残さない**。画面を作り直しても保つ。「すべて開く／すべて閉じる」あり
- **ウィザードは作っていません**（9/16に却下したまま）。1画面の中で畳むことで同じ効果を得た

**つまずき2つ**（どちらも `v2test.js` に再現テストあり）

1. **`.rule` は表彰式の飾り罫のクラス名だった。** カードが幅74px・上下に余白の付いた細い線になった。
   追加ルールのカードは `.rcard` にした
2. **`<details>` の `toggle` は、描画した時点で開いているものにも後から届く。**
   空の大会で警告が出て開いた状態が記録され、サンプルを入れても開いたままになった。
   **描画した状態と同じ通知は記録しない**ようにした

### スコア入力の検算エラーカード（P7）

合わない行ごとに「`90 − 10 ＝ 80 のはず。入れたネットは 79.4（差 +0.6）`」と出し、ボタンを2つ並べます。

| ボタン | 動き |
|---|---|
| **ネットの打ち間違い → 80 に直す** | 入れたネット（`netRef`）を計算値にする |
| **グロスかHDCPを直す** | その人のグロス欄へ移る |

**デザイン案の「集計表が正しい」は採っていません**（4.7の表の5行目）。ネットは順位に使わないので、
集計表のネットが正しいなら誤りはグロスかHDCPです。合わない行には薄い赤の地が付きます。

### 同点の理由を出す（P8）

競技結果の表で、同ネットの行の直後に「`1位と2位は同ネット（80）。年長者を上位にしました（大会設定）`」。
決め方が「決めない」のとき、ハンディも同じとき、生年月日が無いときは、それぞれそう書きます
（生年月日が片方だけ無いと、入っている方が上位になる。これも明記）。
ベスグロが同グロスのときも表の下に理由を出します。

**アプリが勝手に決めたように見えると、表彰の場で説明できない**ためです。

### テスト

`v2test.js` に41項目を追加（204 → 245）。**段階1の `pc.html` に当てると例外で止まる**ことを確認済み。
合計 1541項目（この時点）。`pc.html` は約473KB。上限は4.9で600KBに見直した。

## 4.9 デザイン案どおりにするまでの手順（2026-09-17決定）

### 決めたこと（段階0）

| # | 論点 | 決定 |
|---|---|---|
| 1 | `pc.html` の容量の上限 | **480KB → 600KB**（`mst.js`）。段階2〜8で約80〜100KB増える見込み。**v48の時点で600KB → 700KBに再度見直した（4.12）** |
| 2 | スタート時刻 | **データに持つ**（項目の追加のみ。古いファイルも開ける） |
| 3 | 予想入力の「締め切る」 | **入力を止める**。解除ボタンも付ける |
| 4 | PCの上部タブ | **左サイドバーに置き換える**（二重に並べない） |
| 5 | ホームの「前回の大会から設定をコピー」 | **入れる**。ルール・賞・コースだけを引き継ぐ |
| 6 | 作業環境 | 段階2からは Claude Code の予定だったが、**PCの Claude（チャット）で続行**することにした。zip を上書きアップロードし、チャット側で取得して照合する運用 |

### 手順（1段階＝1回の公開）

| 段階 | 内容 | 状態 |
|---|---|---|
| 1（A） | スマホの集金を1人1カード | **済（v44）** |
| 2 | 骨格：PCの左サイドバーと画面見出し、スマホの下タブの土台。「大会ハブ」「集金」「順位・表彰」「結果共有・出力」を独立させる。既存の11画面のキーは残す | **済（v46）** |
| 3 | 入口：大会ハブの仕上げ（P4・P9。土台は段階2で作成済み）、ホーム（P1）、初回だけの作成画面（P2・P3） | **済（v47）** |
| 4 | 準備：参加者を4列に（P5）、組合せのカード・トレイ・ドラッグ・スタート時刻（P6） | **済（v48）** |
| 5 | 当日と結果：スコア入力の仕上げ（P7。Enterで次の欄、変換中は除外）、順位・表彰の独立（P8） | **済（v49）** |
| 6 | 予想入力：枠を押して選ぶ、締め切りでロック（P10） | **済（v50）** |
| 7 | 発表：1920の配置、次の予告、操作パネルの別ウィンドウ（P11） | **済（v51）** |
| 8 | スマホ：ハブ（M1）、1人ずつの入力と数字キー（M2）、順位カード（M3） | **済（v52）**（参加者の表のカード化は段階9へ） |
| 9 | 仕上げ：使い方の書き直し、印刷、全画面の照合、実機確認 | **済（v53）**（実機確認だけ依頼中） |

**各段階の完了条件**：NG 0・OKの合計が減らない／追加した検査が前の版で落ちる／
デザイン案と同じ画面サイズ（1280×800・390×844・1920×1080）で撮って並べた比較画像を残す／版番号と本書の更新。

**最終の照合では、4.7の表の8項目（意図的にデザインと変えた点）を除いて一致を確かめる。**

### 段階1（A）：スマホの集金カード

760px以下では集金表を隠し、**1人1枚のカード**にする（印刷とPCは従来の表）。

- カード全体が押せる。**カード自体を `button` にし、押したら直接切り替える**（v45）。高さ64px以上
  - v44 では `label` でチェック欄へ転送する作りだったが、**スマホの実機で名前や金額を押しても入らなかった**。
    自動テスト（Chromium のタッチ操作）では再現しない。iPhone の WebKit はこの環境に入れられないため、
    **転送に頼らず、押された要素そのものに処理を付ける**標準的な作りに改めた
- **受け取り済みは地の色・太い金の枠・塗りつぶしの✓・「受け取り済」の札をすべて変える**（v44は色の差が小さく見分けにくかった）。
  押した瞬間は少し縮んで反応を見せる
- 右に徴収額、下に内訳（`実費・原資・馬券◯口・GTO◯口`）。精算がある人は「お渡し」も
- **受け取り済みの数と残りの金額を、画面の下に貼り付ける**。上はアプリの見出しが貼り付いていて隠れるため
- **押したカードの並びは変えない**（次を押し間違える）。代わりに「未集金だけ」で絞る。絞り込みはファイルに残さない
- 内訳は項目ごとに改行しない（`馬券 11` と `口` が別の行に分かれていた）

`v2test.js` に26項目（245 → 271。v45の作り直しで5項目追加）。**段階2の版に当てると例外で止まる**ことを確認。合計 **1541項目**、`pc.html` 約477KB。

## 4.10 段階2：画面の骨格（2026-09-17、v46）

### 画面の一覧（11 → 15）

| キー | 名前 | 段階 | 中身 |
|---|---|---|---|
| `hub` | **大会ハブ**（新） | 段階の外（いつも出る） | 数字のカード・進み具合（押すとその画面へ）・使っているルールの状態（P4・P9） |
| `meta` `players` `groups` `bets` | 大会設定・参加者・組み合わせ・予想入力 | 準備 | 変更なし |
| `money` | 賞金・収支 | **準備**（当日から移動） | 賞金と収支。**集金表は外した** |
| `collect` | **集金**（新） | 当日 | 集金表とスマホの集金カード（`money` から移動） |
| `score` | スコア入力（旧「スコア・順位」） | 当日 | **競技結果の表は外し**、「順位・表彰を見る →」を置いた |
| `prize` | ニアピン・ドラコン | 当日 | 変更なし |
| `rank` | **順位・表彰**（新） | 発表 | 競技結果の表（`score` から移動）。受賞した賞を名前の横に札で出す。技能賞のまとめ。発表の画面へのボタン |
| `sum` `result` | 集計・結果・発表 | 発表 | 変更なし |
| `share` | **結果共有・出力**（新） | 発表 | ファイルに書き出す・結果の文章（コピー可）・印刷する画面への案内 |
| `data` `help` | データ・使い方 | 段階の外 | 変更なし |

**既存の11画面のキーは変えていません**（保存ファイル・テスト・`go()` の呼び出し元が使っている）。
起動時の画面は `meta` → **`hub`** に変えた。「次にやること」に**「順位を確かめる」**（発表の段階の1番目）を足した。

### 置き場所

| 画面幅 | 画面の選び方 |
|---|---|
| **1024px以上（PC）** | **左のサイドバー（236px）**。大会名・大会ハブ・段階ごとの画面と進み数（`16`・`4/4`・`0/16`）・データと使い方。前の段階は1行に畳む。**上のタブ列と上の帯の大会名は隠す** |
| 761〜1023px | 従来どおり上のタブ列 |
| **760px以下（スマホ）** | **下タブ**（大会ハブ＋いまの段階の画面を最大4つ、短い名前と進み数）。上のタブ列も残す（すべての画面へ行ける道を消さない） |

どの画面にも**見出し（画面名・開催日・会場・人数・組数）**を「次にやること」の上に出す。印刷には出さない。
スマホの集金カードの合計の帯は、下タブの上（`bottom:calc(68px + safe-area)`）に移した。

### つまずき

1. **サイドバーが全幅になった。** `#work{display:grid}` を足したが、既存の `body.work #work{display:block}` のほうが強かった。
   **同じ強さ（`body.work #work`）で上書き**した。テストに残してある
2. **描画が遅くなった**（都道府県の選択 3.3ms → 7.3ms、上限5ms）。サイドバー・タブ・帯・下タブで
   「次にやること」の判定と集金の計算を何度もやり直していた。**描画1回の中では結果を使い回す**
   （`FSC`・`STC`・`SCC`）ようにして 3.3ms に戻した。描画の外では毎回計算する

### テスト

- 画面構成の変更に合わせて直したもの：`help.js`・`money.js`（タブの数 11 → 15）、`fee.js`（集金表は `collect`）、
  `hccap.js`（上限の警告は `rank`）、`v2test.js`（段階ごとのタブ・当日の最初の画面・同点の注記は `rank`）
- `v2test.js` に骨格の検査を追加（271 → 322）。**v45 の `pc.html` に当てると例外で止まる**ことを確認
- 合計 **1541項目**。`pc.html` 約497KB（上限600KB）

### デザイン案との比較（1280×800・390×844）

合っている：左サイドバーの構成（大会名・大会ハブ・段階の見出し・進み数・前の段階の畳み・データと使い方）、
画面見出し、大会ハブの「次にやること」＋数字のカード＋チェックリスト＋ルールの状態、下タブ。

**残っている差（後の段階で直す）**
- ~~上の帯：デザインは見出しと保存状態と切替が1行。今は見出しが本文の中にある → 段階3~~ → 済（4.11）
- ~~順位・表彰：デザインは右に技能賞・発表の順番・賞金合計の列がある。今は下に並ぶ → 段階5~~ → 済（4.13）
- ~~スマホの上部：デザインは大会名と「当日 09.19」と切替だけ。今はロゴ・保存状態・タブ列もある → 段階8~~ → 済（4.16）
- デザインのサイドバーの「追加ルール」「発表スクリーン」の項目は、今は大会設定・結果・発表の中 → 段階3・7
  （「追加ルール」は段階3で済。「発表スクリーン」は段階7で「結果・発表」のまま残すと決めた。4.15）

## 4.11 段階3：入口（2026-09-17、v47）

上の帯を1行にし、ホーム（P1）・作成画面（P2・P3）を足し、大会ハブ（P4・P9）を仕上げました。
**作業はPCの Claude（チャット）で行い、zip で受け渡しています**（4.9の決定6）。

### 上の帯（PC、1024px以上）

見出し（画面名・開催日と曜日・会場・人数・組数）・保存状態・段階の切替を**1行（高さ72px）**にしました。
本文の見出しは同じ内容なので、PCでは隠します（760px以下と761〜1023pxは従来どおり本文に出す）。
長い会場名は省略記号で切ります。保存の補足（「終了前にファイル保存を」）はPCの帯では隠します。

開催日は `evDate()` で読みます。「.」「/」「-」「年月日」のどの区切りでもよく、読めなければ曜日を付けません。

### ホーム（P1）

| 置いたもの | 中身 |
|---|---|
| いまの大会 | 大会名・概要・**画面ごとの進みの帯**（済み・途中・まだ）・段階の進み（％）と残り・「つづきから：◯◯ →」・大会ハブへ |
| 開催まで | 開催日から日数（当日は「今日が開催日」、過ぎたら「◯日前に開催」） |
| 新しい大会をはじめる | 白紙から作る／**前回のファイルから設定をコピー** |
| 保存 | この端末の自動保存の状態（復元したときはその旨）・書き出す・読み込む |
| 最近のファイル | **ファイル名と概要（開催日・大会名・人数・組数・優勝者・会場とパー）だけ**を端末に8件まで残す |

**最近のファイルは開き直せません。**オフラインで動くので、ファイルの置き場所を持てないためです（4.7の表の7行目）。
行を押すと「◯◯.json を選んでください」と出して、ファイルを選んでもらいます。
記録は書き出したとき・読み込んだときに付けます（`localStorage` の `golfCompe.recent.v1`）。
**優勝者の氏名もこの端末に残ります。**残したくなければ `recentAdd()` の `win` を外してください。

「つづきから」は「次にやること」と同じ判定です。段階の作業が済んでいれば「『当日』に切り替える」になります。
**起動したときの画面は大会ハブ → ホームに変えました。**

### 作成画面（P2・P3）

**参加者が1名でもいれば開けません**（大会設定へ移ります）。9/16に却下した「毎回のウィザード」にしないためです。
ここで決めることはすべて大会設定・追加ルールと同じデータなので、あとから全部変えられます。段の位置はファイルに残しません。

| 段 | 中身 |
|---|---|
| 1 基本 | 大会名・会場（手入力＋ゴルフ場マスタから選ぶ＋最近使った会場）・開催日（確定で曜日）・1組の人数（3/4/5を押して選ぶ）。右に「この設定でできること」とパーの要約（既定の配分のままなら知らせる） |
| 2 競技方法 | ダブルペリア／新ペリア／ハンディなしを大きな選択肢で。上限・同スコアの決め方などは大会設定と同じ部品（`methodBody(true)`。方式の選択欄だけ省く） |
| 3 追加ルール | 追加ルールの画面と同じカード。「◯個を使用中 → 画面が ＋◯」。「この内容で大会を作る」「ルールなしで作る」（スコア・順位だけ残す） |

- **デザイン案に2段目の絵はありません。**既存の設定を大きな選択肢にして作りました
- 入口は3つ：ホームの「新しい大会を作る」「つづきから：大会を作る」（参加者0名のとき）、
  「次にやること」の「大会の基本を入れる」（参加者0名の間だけ `at:"@make"` で作成画面へ送る）
- 名前・会場の欄は打つたびに画面を作り直しません（落とし穴1）。開催日は確定時だけ作り直して曜日を出します

### 前回のファイルから設定をコピー（段階0の決定5）

| 引き継ぐ | 引き継がない |
|---|---|
| 使うルール・競技方法とパー・技能賞のホール・予想ゲームの単価と方式・罰金・賞の金額・団体賞・抽選賞・集める額の決め方・会場・1組の人数・登録済みのコース | 大会名（**「第N回」を1つ進める**）・開催日・参加者・組・予想・スコア・結果・集金の記録・**収支の実績（income／expense）** |

参加者がいる大会に当てたときは、確認してから上書きし、大会名は変えません。値は複製するので、前回のデータとつながりません。

### 追加ルールを独立させた（15 → 16画面）

デザイン案P4のサイドバーにある「追加ルール」を、大会設定から独立した画面（キー `rules`、準備の段階）にしました。
**同じカードを2か所に出さない**（落とし穴16）ため、大会設定には「使っているルールの札」と「追加ルールを変える →」だけを残しています。
スマホの下タブには出しません（年に数回しか触らないため）。サイドバーの進み数は使っているルールの数です。

技能賞のカードには**対象ホール**を出し、パーと合わなければ「パー3のホールに合わせる」をその場に出します。
カードは `label` なので、ボタンは既定の動きを止めてから直します（押してもチェックは切り替わりません）。

### 大会ハブの仕上げ（P4・P9）

| 左の列 | 右の列（340px） |
|---|---|
| 次にやること（ハブでは本文の帯を重ねず、この列に置く） | 使っている追加ルールの札（押すと追加ルールへ。「＋ 足す」）と、馬券・GTOの有無の説明 |
| 数字のカード3枚 | 次の段階の流れ（準備なら当日＋発表の5つ）。**ルールを足して増えた画面は金色**（P9）。発表の段階では出さない |
| いまの段階のチェックリスト（2列。いま取りかかる行を太字と黄色の輪で） | 集金の見込み（ひとり）。予想が入っていれば「平均」。内訳と丸めの単位 |

数字のカードは段階とルールで入れ替えます。

| 状態 | カード |
|---|---|
| 準備・予想ゲームあり（P9） | 馬券（枠の状態）・GTO（入力済みの口数）・罰金（誰が払うか） |
| 準備・予想ゲームなし（P4） | 参加者（生年月日の数）・組合せ（決まった組／必要な組、**スタート時刻**）・競技方法 |
| 当日・発表 | 集金・スコア・ニアピン・ドラコンの進み |

サイドバーの印も P4 に合わせました。済んだ画面は塗りつぶしの✓、**いま取りかかる画面と途中の画面は黄色の輪**、
手を付けていない画面は輪だけです。ロゴと足元の「ホーム」からホームへ戻れます。
761〜1023px とスマホのタブ列にも「ホーム」を足しました。

### あわせて直したもの（以前からの不具合）

1. **PCで内容が少ない画面だと本文が中央に細く寄っていた**（v46）。grid の中の `.wrap{margin:0 auto}` が
   中身の幅まで縮むため。PCでは `width:100%`・左寄せ・最大1440pxにした
2. **ゴルフ場検索の案内文が「中部5県 379施設・867コース」のままだった**（9/8に全国へ広げたとき）。
   `sel.js` がその文言を検査していたため気づけなかった。全国の収録数に直し、テストも直した
3. 書き出した直後に、サイドバーとホームのファイル名が古いまま残っていた（書き出しのあとに描き直す）
4. 使い方の画面に「使わない機能は『大会設定』で外せます」が残っていた

### テスト

- 直したもの：`help.js`・`money.js`（画面の数 15 → 16）、`sel.js`（収録数の案内）、
  `typing.js`（入力欄を `<label>` から探す。大会設定に「ニアピン ×4」の札が付き、文字だけで探すと札を拾った）、
  `v2test.js`（ルールのカードは追加ルールの画面／畳む準備のタブは6つ／見出しに曜日／起動はホーム／ハブの数字のカード）
- `v2test.js` に**入口の検査を124項目**追加（322 → 446。置き換えで1項目減り、大会設定の札の検査で1項目増えた）。段階3で足した関数は「無ければ null」で受け、
  **前の版に当てても途中で止まらず項目ごとに NG が出る**ようにした
- **v46 に当てると124項目のうち115項目が NG**（直した既存の検査を含めると全体で139項目）。OKの9項目は「〜しない」を確かめる番人で、機能の無い版でも成り立つもの
- `smoke.js` は全画面を描くので、追加ルールの画面の分だけ1項目増えた
- 合計 **1666項目**（1541 ＋ 入口の124 ＋ smoke の1）。`pc.html` 約537KB（上限600KB）
- **実際のブラウザ（Chromium）で51項目**を通した（リポジトリには置いていない）：作成画面で1文字ずつ打ってもフォーカスが外れない／
  ゴルフ場マスタから選ぶと会場とパーが入り、開いた欄が閉じない／合わせるボタンでチェックが変わらない／
  書き出し → 最近のファイル → 新しい大会 → 前回からコピー（第N回が進む）→ 最近のファイルから読み込み／
  再読み込みでホームと復元の知らせ／帯の高さ72px・サイドバー236px・本文が列いっぱい／
  1024px と 1023px の切り替わり／390px の7画面で横にはみ出さない

### デザイン案との比較（1280×800・390×844・1920×1080）

比較画像は**受け渡しの zip に同梱し、リポジトリには置きません**（デザイン案の絵に実在のゴルフ場名が入っているため）。

合っている：ホームの2列の構成、作成画面の段の表示・基本4項目・右の説明・追加ルールのカード、
大会ハブの左右の列・数字のカード・チェックリスト・追加ルールの札・次の段階の流れ・集金の見込み、上の帯の1行、サイドバーの印。

**意図して変えたもの**（同じ提案が来たら読み返すこと）

| デザイン案 | v47 | 理由 |
|---|---|---|
| 金額は「¥200」 | 「200円」 | アプリ全体の表記に合わせた |
| 最近開いたファイルから直接開く | 名前を示して選んでもらう | オフラインでは置き場所を持てない（4.7の表） |
| 追加ルールは8枚 | 11枚を3つのまとまりで（スコア・順位を含む） | 現行のルールをすべて切り替えられるように |
| 当日の流れに「予想入力」「集計」 | 入らない | 予想入力は準備、集計は発表の段階（4.10） |
| ヘッダーに「コースマスタ」 | 「データ」「使い方」 | コースマスタは大会設定の中にある |
| 数字のカードの「新ペリア」 | 既定はダブルペリア | 4.7の表の6行目 |

**残っている差（後の段階）**：スマホの上部（段階8）、順位・表彰の右の列（段階5）、発表スクリーン（段階7）、
チェックリストの「スタート表を印刷」（組合せの段階4で判定を足す）。

### 決めたこと（2026-09-17、v47の公開後にユーザーが確認）

実装のときに仮に置いた6点を、**すべてそのまま採る**と決めました。同じ提案が来たら読み返すこと。

| # | 決めたこと | 理由 |
|---|---|---|
| 1 | 起動画面はホーム（代案「復元したときだけ大会ハブ」は採らない） | 当日も「つづきから：集金する」の1押しで戻れる |
| 2 | 追加ルールは独立した画面（16画面） | 同じカードを2か所に出さない（落とし穴16） |
| 3 | 準備中に予想ゲームを使うと、ハブの数字のカードは馬券・GTO・罰金（参加者は外れる） | P9どおり。人数はサイドバーに出ている |
| 4 | 金額は「円」表記 | アプリ全体に合わせる |
| 5 | 前回からコピーすると「第N回」を1つ進める | 開催日と同じく、毎回直す手間を省く |
| 6 | 最近のファイルに優勝者の氏名を残す | この端末の `localStorage` だけ。どの年のファイルかを見分けやすい |

### 実機での確認（2026-09-17、iPhone、v47）

ホーム画面のアイコンを入れ直してから、次の3点を確かめて**問題なし**。

1. ホームの「最近のファイル」から選んで開ける（iPhone のファイル選択）
2. 作成画面で日本語入力が崩れない
3. 技能賞カードの「合わせる」ボタンを押しても、スイッチが切り替わらない

GitHub の main（v47）は、照合用の md5 で38ファイルすべて一致、余分なファイルなし、25本・1666項目・NG 0 を確認した。

## 4.12 段階4：準備（2026-09-17、v48）

参加者（P5）と組合せ（P6）を作り直しました。**始める前に3点を決めました。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | 参加者の表に入りきらない列（枠・実力・参加のしかた・原資・個別額） | **表の上で列のまとまりを切り替える**（基本／枠と実力／参加とお金） |
| 2 | スマホで人を組へ移す方法 | **押して行き先の組を選ぶ＋長押しで運ぶ**（組ごとの選択欄は置き換える） |
| 3 | 容量（537KB／600KB） | 段階4はそのまま進め、超えそうになったら判断 → v48で556KB（残り約44KB）になったので、**上限を700KBに上げると決めた**（下の節） |

### 参加者（P5）

| 置いたもの | 中身 |
|---|---|
| 上の操作 | 列の切替（基本／枠と実力／参加とお金）・「＋ 1名追加」・「次へ：組合せ →」。下に人数と入力の進み（生年月日・所属） |
| 表 | 基本は **No.・氏名・所属・生年月日・組** と、並べ替え（▲▼×を1つの欄に） |
| 右の列（340px） | **貼り付けて取り込む**（主役）と、**生年月日はなぜ必要？**（同スコアの決め方に合わせて文面が変わる） |

- **列は削っていません。**すべて描いて、見せる列だけを CSS で選びます（`.ptab .col` と `.pwrap.v-b/v-f/v-m`）。
  既存の検査（`fee.js` の原資・個別額・参加のしかた）はそのまま通ります
- 「枠と実力」は**馬券を使い、枠を自分で決めるときだけ**出します（枠が組と同じなら、基本に「枠は組と同じ番号」と出す）
- 枠が決まっていない方がいると「枠と実力」に●、車を出す方が未選択なら「参加とお金」に●
- 「次にやること」の「馬券の枠を決める」は、枠を自分で決める運用なら `at:"@pcol-f"` で「枠と実力」を開いて送る
- 選んだまとまりはファイルに残しません（`PCOL`）
- 取り込みの確認は、右の列に収まるよう**表ではなく1行ずつの並び**にしました（`bd.js` を合わせた）
- 空の大会では貼り付けを先に出します（1列に畳んだとき上）

### 組合せ（P6）

| 置いたもの | 中身 |
|---|---|
| 上の操作（`id="g-start"`） | 「◯組 × ◯名 ・ ◯名が未割り当て」／**スタート [8:00] から [7] 分間隔 [全組に時刻を入れる]**／**残りを自動で割り当てる**／スタート表を印刷 |
| 組のカード | 組番号・スタート時刻（その場で直せる）・人数・削除。人の下に**空き**を点線で出す。人のいない組は「ここへ運ぶ（4名）」 |
| 右のトレイ（260px、狭い画面では上） | **未割り当て**（組を待っている人）と、その下の段に**組から外した方・予想のみの方** |
| 下の操作 | ＋ 組を追加／全員を上から順に割り当て直す（従来の `gAuto`）／割り当てを解除 |

**人の動かし方は3つ。すべて `gSetG()` を通る**ので、枠と原資の扱いがそろいます。

| 動かし方 | 作り |
|---|---|
| ドラッグ（PC） | 従来の HTML5 ドラッグ。落とし先はカードとトレイ（`data-g`） |
| **押して選ぶ** | 名前（`button`）を押すと、画面の下に行き先の欄（`gsheet`）。満員の組といまの組は押せない。「＋ 新しい組」「組から外す」。Escで閉じる |
| **長押しで運ぶ** | 450ms 動かさずに押し続けると運ぶ状態（名札が指の上に出る）。それより前に指が動いたらスクロール扱い。運んでいる間はスクロールを止め（`touchmove` を `passive:false` で受ける）、離した組へ移す。組の外なら何もしない。2本指では始めない。運んだ直後の「押した」は無視する |

- **長押しは iPhone の実機で確かめていません**（この環境の Chromium でタッチ操作を再現して確認）。
  落とし穴22のとおり、確実に使える道として「押して選ぶ」を必ず残しています
- 名前には `-webkit-touch-callout:none`・`user-select:none`・`touch-action:manipulation` を付け、長押しでメニューや文字選択が出ないようにした

**「残りを自動で割り当てる」（`gFill`）は、組の決まっていない方だけを空いている組へ上から入れます**（足りなければ組を足す）。
決めた組は動かしません。全員を並べ直す従来の `gAuto` は「全員を上から順に割り当て直す」として残しました（予想のみの方も入れてしまう点は従来のまま）。

**スタート時刻**：`meta.startAt`（最初の時刻）と `meta.startGap`（間隔の分。1〜60、既定7）を足しました（段階0の決定2。項目の追加のみで、古いファイルは既定値で開く）。
各組の時刻は従来どおり `groups[].time`。「8:00」「８：０５」「8時5分」「7.30」を読み、日をまたいでも崩れません。
サンプル①は 8:00 から 8分間隔。**「次にやること」に「スタート時刻を入れる」を足しました**（人のいる組すべてに時刻があれば済み。準備の手順は8つに）。

### 組から外した方がトレイから消えかけた（設計の要点）

**組から外すと原資も外れる**のが9/8からの決まりです（`touch.js` が検査）。そのため、組から外した方は
データの上では「予想のみ」（枠なし・投票あり・原資なし）と区別がつきません。
最初の実装ではトレイに「組の決まっていない方」だけを出していたので、**トレイへ運んだ人が画面から消えました**。

お金の決まりは変えず、トレイを2段にしました。

- 上の段：**未割り当て**（原資の対象のまま組を待っている方。新しく名簿に足した方など）。自動の割り当ての対象。「次にやること」の「残り◯名」と同じ数
- 下の段：**組から外した方・予想のみの方**。押す・運ぶで組に戻せる（戻すとプレーする扱いに戻る）。自動の割り当てには入れない

行き先の欄のボタンも「未割り当てへ」ではなく**「組から外す」**にしました（実際に原資が外れるため）。

### 容量の上限を700KBに（2026-09-17、v48の公開前に決定）

段階2〜4で約80KB増え（477KB → 556KB）、段階5〜8で600KBを超える見込みが高くなったため、次の3案から **A** を選びました。

| 案 | 内容 | 判断 |
|---|---|---|
| **A** | 上限を700KBに上げる | **採る。**単一ファイル・オフライン・USBで持ち運ぶ設計には影響しない。手間が最小 |
| B | 重複したCSSなどを整理して軽くする（20〜30KBの見込み） | 見送り。見た目が崩れていないかの照合に1回ぶんの作業がかかる |
| C | 600KBに達してから決める | 見送り。段階の途中で止まる |

`mst.js` の検査を「600KB 未満」→「**700KB 未満**」に変えた（件数は変わらない）。
**700KBも超えそうになったら、そのときはBを検討する。**上限を上げ続けるのではなく、段階8までの見込みで決めた値です。

### あわせて直した不具合

1. **ドラッグで未割り当てへ落としても枠と原資が残っていた**（枠を自分で決める運用）。`gpDrop` が `p.g` だけを変えていた。`gSetG` を通すようにした
2. **スマホで参加者の画面が横にはみ出していた**（390px で627px）。1列に畳むグリッドを `1fr` にしていたため、表の最小幅まで広がった。
   `minmax(0,1fr)` にした（大会ハブ・組合せも同じく直した）
3. 「参加のしかた」の選択肢が、狭い表の中で「プレー＋予」で切れた。見出しの下限幅を 11em → 12.5em にした
4. 使い方の画面の手順3を、新しい操作（自動の割り当て・押して選ぶ・長押し・全組に時刻）に合わせた

### テスト

- 直したもの：`bd.js`（取り込みの確認は1行ずつの並び）、`design.js`（空の表の案内文）、
  `resp.js`（組の選択欄 → 押して選ぶ・長押し。+1項目）、`v2test.js`（準備の手順にスタート時刻、枠の送り先）
- `v2test.js` に**準備の検査を90項目**追加（446 → 539。手順の検査で+3）。前回と同じく、新しい関数は「無ければ null」で受ける
- **v47 に当てると90項目のうち78項目が NG**、途中で止まらない。OKの12項目は「列を削らない」「組の外で離しても動かない」など、機能の無い版でも成り立つ番人
- 合計 **1760項目**（1666 ＋ `v2test.js` 93 ＋ `resp.js` 1）。`pc.html` 約556KB
- **実際のブラウザ（Chromium）で33項目**（リポジトリには置いていない）：列の切替で見える見出しが入れ替わる／表がカードに収まる／
  氏名を打ってもフォーカスが外れない／貼り付け → 右の列の確認 → 取り込み／ドラッグで組へ／押して選ぶ（満員は押せない・Escで閉じる）／
  組から外した人がトレイの下の段に残り、そこから戻せる／全組に時刻・組ごとに直す／
  **スマホ（タッチを再現）：すぐ動かすと運ばない・押すと行き先の欄（下タブに重ならない）・長押しで運ぶ状態・運ぶ間は画面が動かない・離した組へ移る・運んだあとに欄が出ない**／
  390px で横にはみ出さない。段階3の51項目も再実行して NG 0
- **ブラウザ検査の組み方で2回つまずいた**（落とし穴28）。スマホでは上に貼り付いた見出し（約200px）の下に隠れた名前を押していた／
  「次にやること」の説明文にある同じ文字を、ボタンのつもりで押していた

### デザイン案との比較（1280×800・390×844・1920×1080）

比較画像は受け渡しの zip に同梱（リポジトリには置かない。4.11と同じ理由）。

合っている：参加者の基本4列・右の貼り付けと「なぜ必要？」・1名追加と次へ、組のカード（番号・時刻・人・空き）・右のトレイ・自動の割り当て・スタートの間隔・印刷。

**意図して変えたもの**

| デザイン案 | v48 | 理由 |
|---|---|---|
| 操作（1名追加・自動の割り当て・印刷）は上の帯 | 本文の最初（「次にやること」の下） | 上の帯は全画面共通の「見出し・保存・切替」（4.11）。画面ごとの操作を入れると1行に収まらない |
| 参加者の表は5列だけ | 5列＋切替で「枠と実力」「参加とお金」 | 列を削らない（9/16の方針）。決定1 |
| 行の「⋮」メニュー | ▲▼× | スマホでドラッグできない行の並べ替えを残す |
| トレイは未割り当てだけ | 下の段に「組から外した方・予想のみの方」 | 組から外すと原資が外れる決まりのため（上の節） |
| 「上から順に割り当てる」 | 「残りを自動で割り当てる」（トレイの下にも） | 決めた組を動かさないことを名前で示す |

**残っている差（後の段階）**：スマホの参加者の表のカード化（段階8）、スマホの上部の高さ（段階8。ブラウザ検査でも押す位置に効いた）。

### 公開と実機での確認（2026-09-17、v48）

GitHub の main（v48）は、照合用の md5 で38ファイルすべて一致、余分なファイルなし、25本・1760項目・NG 0 を確認した。
iPhone（ホーム画面のアイコンを入れ直して）で次を確かめて**問題なし**。

1. 組合せで名前を長押しして運べる（スクロールと取り違えない・メニューや文字選択が出ない）
2. 名前を押して行き先の組を選べる
3. 参加者の表の列の切替（基本／枠と実力／参加とお金）
4. スタート時刻の一括入力

## 4.13 段階5：当日と結果（2026-09-17、v49）

スコア入力（P7）と順位・表彰（P8）を作り直しました。**始める前に3点を決めました。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | スコア入力の配置（隠しホール・18ホール方式は列が多い） | **集計表方式だけ右の列**。隠しホール／18ホールは表を全幅 |
| 2 | キーボードの動き | **Enter＝次の欄**、Tab はブラウザの標準のまま（デザイン案の「Tab で次の人」は採らない） |
| 3 | 順位表の受賞の出し方 | **「賞」の列を足す（印刷にも出す）**。表彰の控えになる |

### スコア入力（P7）

| 置いたもの | 中身 |
|---|---|
| 上 | 入力の進み（帯と「13 / 16 入力済」）、確定の条件（集計表方式は「× 1行 ・ 全行○で確定」）、**入れ方の切替**（集計表から／隠しホール＋グロス／18ホールすべて） |
| 表 | **組**・氏名・グロス・HDCP・ネット・**検算**・順位。集計表方式の欄は高さ38pxで大きく |
| 右の列（集計表方式だけ、340px） | 打ち込みの検算（状態と、合わない行ごとの直し方のカード）／**集計表を貼り付けて取り込む（普段は畳む。全員が空なら開く）**／入力が終わったら（順位・表彰へ） |

検算の列の出し方：

| 状態 | 出し方 |
|---|---|
| 3つとも入って一致 | ○ |
| 合わない | 計算値に「?」（例 `80?`、差は title）。**ネットの欄を赤枠**、行を薄い赤 |
| グロスとHDCPだけ（ネットなし） | 計算値を薄く（集計表と見比べられる） |
| 途中まで | … |
| 何も入っていない | — |

- 列の名前は「照合」→「**検算**」（デザイン案に合わせた。カードの名前「打ち込みの検算」とそろう）
- 以前の「ネット（計算値）」と「集計表のネット（入力）」の2列は、「ネット（入力）」と「検算」の2列にまとめた
- 貼り付けの確認（`#pv`）は右の列に収まらないので、表の上に全幅で出す
- **デザイン案の「集計表が正しい」ボタンは採っていません**（4.7の表の5行目）
- 隠しホール／18ホール方式では、全幅の表の下に従来の「順位」のカードを置く
- 数値欄の増減ボタン（スピナー）は集計表方式では出さない（「10.1」が「10.(」と切れた）

**Enter で次の欄**（4.7で決めた「数値欄だけ、`isComposing` を除外」）：

- 各欄に `data-r`（行）・`data-c`（列）を付け、`onkeydown="scKey(event)"` で受ける
- 行き先：同じ人の次の欄 → 行の終わりなら次の人の最初の欄。**Shift＋Enter で前の欄**（行の頭なら前の人の最後の欄）
- **確定（change）で画面を作り直すので、行き先を決めてから `blur()` で確定し、作り直したあとの同じ位置の欄へ入る**（値を変えていなければ作り直さずに移る）
- 拾わない：`isComposing`・`keyCode 229`（日本語の変換を確定する Enter。落とし穴2）、Alt・Ctrl・⌘ と一緒の Enter、Tab
- 最後の人の最後の欄では確定だけ

### 順位・表彰（P8）

| 置いたもの | 中身 |
|---|---|
| 上 | 「全員入力済 ・ いまの1位 ◯◯」／競技結果を印刷／**▶ 発表の画面へ** |
| 左（順位表） | 順位・氏名（**所属は名前の下**）・グロス・HDCP・ネット・**賞**。**上位3名は行を高く・字を大きく**、1位は淡い金の地。同ネットの理由・ベスグロの理由・要約・反映ボタンは従来どおり |
| 右の列（300px） | **技能賞**（ホールごとに受賞者、未定は黄色）／**発表の順番**（区切りと枚数）／**賞金合計**（内訳、原資と残り。足りなければ赤） |

- 賞の列：賞金の設定にある賞（金額つき）。ベスグロは賞金の設定に無くても出し、設定にあれば1つだけ
- 所属の列は外した（名前の下に出す）。表の列数は6のまま（同点の行の `colspan` は変わらない）
- **印刷**：1列に並べ、右の列は出さない。賞の列は黒で出す
- **発表の順番**は `showPlan()`。`buildSlides()` を呼んで識別子から区切り（開幕・ニアピン・何でもニアピン・ドラコン・競技結果・団体賞・賞・馬券・GTO・選抜者・罰金・抽選・締め）にまとめる。
  **発表中の状態（`slides`・`S`）は作ったあとに元へ戻す**。枚数は実際の発表と一致する（テストで確認）
- **「▶ 発表の画面へ」は従来どおり結果・発表の画面へ移るだけ**。当選枠の反映や手動指定がその画面にあるため。デザイン案の「発表をはじめる」は段階7で見直す

### テスト

- 直したもの：`resp.js`（スコア入力表の最初の列は「組」）、`sheet.js`（列の名前は「検算」）、`v2test.js`（受賞は賞の列に）
- `v2test.js` に**当日と結果の検査を57項目**追加（539 → 596）。Enter の検査は、器に「入力欄を探す関数」を足し、
  **確定 → 作り直し → 次の欄に入る**の順番と、変換中・229・Tab・Ctrl を拾わないことを確かめる
- **v48 に当てると57項目のうち49項目が NG**、途中で止まらない。OKの8項目は「変換中は拾わない」「Tab は標準のまま」「貼り付けは出さない」など、機能の無い版でも成り立つ番人
- 合計 **1817項目**（1760 ＋ 57）。`pc.html` 約567KB（上限700KB）
- 検査の書き方で1つ直した：正規表現の途中の `|` で条件が2つに割れ、前の版でも通っていた（落とし穴29）
- **実際のブラウザ（Chromium）で30項目**：打って Enter でグロス → HDCP → ネット → 次の人／3つ入れると○、進みが増える／
  打ち間違えると赤い欄と「78.6?」、右の列にカード、入力位置は次の人に残る／値を変えずに Enter でも移る／Shift＋Enter／Tab は標準／
  変換中（`isComposing`）と229では移らない／直すとカードが消える／表と右の列が並ぶ（340px）／隠しホールは全幅、ホールでも Enter／
  順位表と右の列（300px）、表がカードに収まる、上位の行が高い／**印刷の表示で賞の列が出て右の列は出ない**／発表の画面へ移る／
  390px の3画面で横にはみ出さない。段階3（51項目）・段階4（33項目）も再実行して NG 0

### デザイン案との比較（1280×800・390×844・1920×1080）

比較画像は受け渡しの zip に同梱（リポジトリには置かない。4.11と同じ理由）。

合っている：入力の進み、組・グロス・HDCP・ネット・検算の表、合わない行の赤枠と「?」、右の列（検算カード・畳んだ貼り付け・入力後の案内）、
順位表（上位を大きく・所属は名前の下・賞の列）、右の技能賞・発表の順番・賞金合計。

**意図して変えたもの**

| デザイン案 | v49 | 理由 |
|---|---|---|
| 検算カードの「集計表が正しい」 | 「グロスかHDCPを直す」 | ネットは順位に使わない（4.7） |
| Tab で次の人 | Tab はブラウザの標準 | 決定2 |
| 入れ方の切替なし | 上で切り替える | 3方式を残す（大会ごとにゴルフ場の出す物が違う） |
| 発表の順番「ブービー → … → 優勝 → ベスグロ」 | 9/8確定の並び（技能賞 → 競技結果 → … → 馬券 → GTO） | 4.7の表の1行目 |
| 「発表をはじめる」ですぐ始まる | 結果・発表の画面へ | 反映と手動指定がそこにある。段階7で見直す |
| サイドバーの「発表スクリーン」 | まだ無い | 段階7 |

**残っている差（後の段階）**：発表スクリーン（段階7）、スマホの1人ずつの入力と数字キー（M2）・順位カード（M3）（段階8）。

## 4.14 段階6：予想入力（2026-09-17、v50）

予想入力（P10）を作り直し、締め切りを入れました。**始める前に3点を決めました。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | 「次にやること」の「予想を入力する」はいつ済むか | **締め切ったら済む**（馬券・GTOの代金は締め切ってから集めるため。4.7） |
| 2 | 馬券の人気のマス目（それまでは「集計」だけ） | **予想入力の右に出し、集計でも当選の印つきで残す** |
| 3 | 馬券とGTOの見せ方 | **画面の中で切り替える**（P10どおり） |

### 画面

| 置いたもの | 中身 |
|---|---|
| 上 | 馬券（枠連）／GTO（下位予想）の切替（口数と、無効の口があれば「無効◯」の印）・「紙の予想を貼り付け」・**「締め切る」**（締め切り後は「締め切り済 21:30」と「解除」） |
| 左：選ぶ | **馬券**：枠のボタン（枠の色の帯・番号・「青木 ほか1名」）を2つ押す。同じ枠をもう一度押すとゾロ目（×2）。3つ目を押すと選び直し。<br>**GTO**：出走する方のボタンを押した順に 最下位 → 下位2位 → 下位3位（番号の印）。もう一度押すと外れて順が詰まる。3名そろうとほかは押せない。「選び直す」<br>どちらも：投票者・口数（− ＋）・「3-5 を追加（Enter）」（選び終えるまでは押せない） |
| 左：一覧 | 従来の表。下に「◯件」と「未投票 ◯名：…」。統計の行も従来どおり |
| 左：貼り付け | 「紙の予想を貼り付けて取り込む」（普段は畳む。上のボタンで開く） |
| 右（300px） | **馬券**：枠の人気のマス目（選んでいる組合せに枠、一番人気は黄色）と一言、売上、GTOの進み。<br>**GTO**：得票の多い方（★は罰金の基準の上位）、売上と方式、馬券の進み |

- 選んだ枠・人・口数・投票者・開いている種類はファイルに残さない（`BTAB`・`BSEL`・`GSEL`・`BV`・`BQ`・`BPASTE`）
- **`kAdd`・`gAdd` は従来どおり `ka_*`・`ga_*` の欄から読む**。押して選んだものは隠し欄（`ka_a`・`ka_b`・`ga_1〜3`）に入れる。既存の検査がそのまま通る
- 追加したら投票者はそのまま、枠・人・口数は選び直し（同じ人が続けて何口も買うため）
- 枠や人を選ばずに追加すると「枠を2つ選んでください」「3名を選んでください」と出す
- 口数の欄の Enter は変換中（`isComposing`・229）を拾わないようにした（以前は見ていなかった）
- マス目は `kGrid()` 1つを予想入力と集計で使う（集計は当選の印、予想入力は選択と一番人気の印）。右の列に8枠が収まるよう、予想入力ではマスの下限幅を 32px → 26px にした

### 締め切り（4.9の決定3）

- データ：`meta.betsClosed`（既定 false）と `meta.betsClosedAt`（押したときの「時:分」）。項目の追加のみで、古いファイルは既定値で開く
- 締め切ると、**追加・口数の変更・削除・貼り付け（読み取りと取り込み）を止める**。関数の入口で止めるので、画面以外から呼んでも変わらない。
  押すと「予想は締め切っています。直すときは「解除」を押してください。」と出す
- 画面：枠・人・追加・口数・投票者・貼り付けを押せなくし、薄く出す。一覧の口数は変えられず、×は出さない
- 締め切りと解除はどちらも確認してから（解除は「予想を直すと集める額が変わる」と添える）
- **集金の画面**：予想を使っていて締め切る前なら「予想がまだ締め切られていません。代金は締め切ってから集めます」と出し、予想入力へ送る
- **サンプル①は締め切った状態**（21:30）にした。本番の稽古として「予想を入れ終えた」状態のため。
  サンプルに予想を足す検査（`bets.js`・`void.js`・`v2test.js` の4章）は、最初に解除するよう直した
- 4.7で採らないと決めた「締切まで◯分」「口数の上限＝集金済みの口数」は入れていない

### テスト

- 直したもの：`bets.js`（解除してから取り込む。貼り付け欄は切り替えてから見る）、`void.js`（解除してから追加する。無効の印はGTOに切り替えて見る）、
  `v2test.js`（4章は解除してから。手順の検査に「1口入れても締め切るまでは済まない」を足した）
- `v2test.js` に**予想入力の検査を70項目**追加（596 → 667。手順で+1）
- **v49 に当てると70項目のうち63項目が NG**、途中で止まらない。OKの7項目は「締切まで◯分は出さない」などの番人と、
  v49でも結果が同じになる項目（サンプルに予想があるので「済み」になる。締め切りで判定していることは「締め切る前は済まない」が v49 で落ちることで押さえた）
- 合計 **1888項目**（1817 ＋ 71）。`pc.html` 約582KB（上限700KB）
- **実際のブラウザ（Chromium）で26項目**：投票者を選んだまま枠を選べる（描き直しで戻らない）／＋で口数／追加でまとめる・選び直し／口数の欄で Enter／
  変換中の Enter では追加しない／貼り付けが開く／GTOは3名で追加でき4人目は押せない／押した順で入る／締め切ると押せない・薄い・×が消える／
  集金の知らせ／解除／右の列300px・マス目が収まる／390px の3画面で横にはみ出さない。段階3〜5（計114項目）も再実行して NG 0

### デザイン案との比較（1280×800・390×844・1920×1080）

比較画像は受け渡しの zip に同梱（リポジトリには置かない）。

**意図して変えたもの**

| デザイン案 | v50 | 理由 |
|---|---|---|
| 「締切まで12分」 | 出さない（「締め切る」ボタンだけ） | 締切は前夜に会長が決める。時刻を持たない（4.7） |
| 「5口まで（集金済）・残り2口」 | 出さない | 代金は締め切ってから集める（4.7） |
| サイドバーの「集計（馬券・GTO）」が当日 | 集計は発表の段階 | 4.10の構成 |
| 枠のボタンに「佐藤 ほか3名」 | 「青木 ほか1名」（枠は2人ずつ） | 組と枠は別物（4.5） |
| GTOの画面の絵なし | 人のボタンを押した順 | 「最下位から順に」はデザイン案の説明どおり |

**残っている差（後の段階）**：スマホの予想入力の仕上げ（段階8）。

## 4.15 段階7：発表（2026-09-17、v51）

表彰式の画面（P11）を 1920×1080 向けに組み直し、操作パネルを別ウィンドウにしました。**始める前に3点を決めました。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | 幹事の操作パネル | **別ウィンドウで開く。開いている間はスクリーンの操作の帯（HUD）を隠す** |
| 2 | 次の予告に出すもの | **スクリーンは賞の名前だけ。操作パネルには次の受賞者名も出す**（読み上げの準備のため） |
| 3 | 順位・表彰の「発表をはじめる」 | **結果を反映済みならその場で始める。順位が変わっていたら知らせて反映へ** |

「結果・発表」の画面の名前は変えていません。当選枠・GTOの下位3名・罰金の手動指定の入力がこの画面にあり、
デザイン案の「発表スクリーン」にすると入力の場所が分かりにくくなるためです。

### 1人を発表する画面（2列）

対象：優勝・2位・3位、ベストグロス、各賞（`s-pz`）、技能賞の各ホール、GTOの結果（第1〜3位）。`winSlide()` で作る。

| 左 | 右 |
|---|---|
| 見出し（従来の「第 3 位」「優　勝」など。`kicker`）・**大きな数字**（順位・打数・ホール番号。賞は賞の名前）・補足（同ネット／同グロスの理由、「ネットの下から 2 番目」、「OUT ・ パー3」、「打数の多い方から」） | **所属・名前・成績（ネット／グロス／HDCP、技能賞は記録、GTOはグロス）は伏せておき、押すと現れる**（`id="veil"`）。**賞金は伏せずに見せる**（「3位賞 3,000円」） |

- 同ネットの理由は `tieWhy()` と同じ文面（「ネット同スコア（80）。年長者を上位にしました（大会設定）」）
- 賞金の札は、順位賞の設定にある金額（「優勝賞」「2位賞」…）。設定に無ければ従来どおりその人の賞金の合計
- 伏せている間は名前だけでなく所属と成績もぼかす（所属や成績で分かってしまうため）
- 760px 以下では1列に戻す
- 既存の検査が見る `kicker`・`id="veil"`・`name-xl`（名前だけを直接入れる）は残した

### スクリーンの枠と次の予告

- 上：大会名・日付・会場と「15 / 37」
- 下：**「← 前の発表」**（伏せていた名前が出る画面の後なら名前も）・進みの点（済み・いま）・**「次：2位 →」**（賞の名前だけ。受賞者名は出さない）。最後は「最後の画面です」
- 各画面に見出し（`lab`）と受賞者（`who`）、伏せる画面の印（`rv`）を持たせた。字間の空白は取り除いて読みやすくした
- 操作の帯が出ているとき（パネルが無いとき）は、下の枠を帯の上にずらす
- 帯の中の進みの点（`#dots`）も残した（`sound.js` が見ている）

### 操作パネル（別ウィンドウ）

- 「結果・発表」の「操作パネルを開いて発表をはじめる」、順位・表彰の「操作パネルつきで始める」、スクリーンの操作の帯の「操作パネル」から開く
- **空のページを開き、DOM で題名・スタイル・本文を差し込む**（`window.open("","compePanel")` → `panelBuild()`）。同じ出どころになるので、
  ファイルを直接開いても・オフラインでも、パネルのボタンからスクリーン側の関数（`advance`・`step`・`toggleSound` など）をそのまま呼べる。
  通信や BroadcastChannel は使わない（ファイルを直接開いたときに届かないことがあるため）
- **スクリプトの中に文書の骨組みの札（doctype・html・head・body）を書かない**。初版では `document.write` に文書まるごとの文字列を渡しており、
  Claude のプレビューが崩れた（下の「公開前に見つけて直したこと」）
- キーの受け手とパネルを閉じたときの処理は `onkeydown`・`onpagehide` で1つだけにした（開き直すたびに増えないように）
- パネルの中身：大会名、「15 / 37」、**いま**（見出しと受賞者）、**次**（見出しと**次の受賞者名**）、「← 戻る」「次へ →」（大きく）、音の切替・音量、作業画面にもどる、
  全画面の一覧（押すとその画面へ。先の受賞者名は一覧には出さない）。← → スペースで送れる
- 開いている間はスクリーンの操作の帯を隠す（`#hud.off`、`#show.paneled`）。パネルを閉じると帯が戻る（`pagehide`）
- 作業画面に戻ると、パネルは「いまは発表していません」と「▶ 発表をはじめる」「閉じる」になる
- 開けなかったとき（ポップアップの禁止など）は「ブラウザでこのページのポップアップを許可してください」と出す
- **スクリーン側を読み込み直しても開き直せる**。ファイルを直接開いているときは、読み込み直すと残っていたパネルが別の出どころになり、
  触れると例外になる。触れないパネルは閉じて同じ名前で開き直す（閉じきる前の同じウィンドウが返るブラウザでは別の名前で開く）
- **スクリーンの全画面は、スクリーン側で F キー**（ブラウザの決まりで、別ウィンドウのボタンからは切り替えられない）。
  **先にパネルを開いてから、発表の画面をプロジェクター側へ移して F** の順が確実（使い方の画面にも書いた）

### 発表をはじめる（反映済みかどうか）

- `resultStale()`：いまの順位から出る「馬券の上位2名・当選枠」「GTOの下位3名」「罰金のグロス」と、結果・発表の入力を比べ、合っていない項目の名前を返す（スコアが無ければ「スコア」）
- 順位・表彰の上：反映済みなら「**▶ 発表をはじめる**」と「操作パネルつきで始める」。**合っていなければ「結果を反映して発表の準備へ」と、どれが合っていないかの知らせ**。
  「結果・発表の画面へ」はいつも出す
- 結果・発表の④にも同じ知らせと「順位から反映し直す」
- スコアを直したあとに反映し忘れて、古い結果で発表してしまうのを防ぐため

### 公開前に見つけて直したこと（2026-09-17）

1. **Claude のプレビューで `pc.html` が崩れた**（ユーザーがスクリーンショットで指摘。GitHub へ上げる前）。
   操作パネルの中身を「文書まるごとの文字列」（`<!doctype html><html><head>…<body>…</body></html>`）としてスクリプトに書いていた。
   `pc.html` はもともと html・head・body の札を省いた書き方で、v50 までファイルにこれらの札は1つも無い。
   v51 の初版では、ファイルでただ1つの body の札がスクリプトの中の文字列になり、プレビューがそこを本文の始まりと取り違えて、
   以降のスクリプトを文字として表示した。**ブラウザはスクリプトの中身を `</script>` まで文字として読むので、ブラウザでの検査は通っていた。**
   DOM で差し込む形に直し、`design.js` に「骨組みの札をどこにも書かない」検査を足した（初版に当てると落ちる）
2. **スクリーン側を読み込み直すと操作パネルが開けなくなっていた**（1を直したあとのブラウザでの確認で発覚。初版からあった）。
   ファイルを直接開いたときの出どころの扱いによる。触れないパネルを閉じて開き直す形にした。読み込み直しを3回続けても、ページはスクリーンとパネルの2つのまま

### テスト

- 直したもの：`money.js`（賞金は「優勝賞 10,000円」の札）、`v2test.js`（順位・表彰の上のボタン）
- `v2test.js` に**発表の検査を61項目**追加（667 → 728）。別ウィンドウは、差し込まれた HTML から要素を作る器で真似て、
  ボタン・キー・一覧・閉じたときの戻り・残っていたパネルへの入れ直し・触れないパネルの開き直しを確かめる
- `design.js` に「骨組みの札をどこにも書かない」を追加（37 → 38）
- **v50 に当てると61項目のうち54項目が NG**、途中で止まらない（骨組みの札の検査は v50 では通り、v51 の初版では落ちる）
- 合計 **1950項目**（1888 ＋ 61 ＋ 1）。`pc.html` 約601KB（上限700KB）
- **実際のブラウザ（Chromium）で32項目**：パネルつきで始める／パネルが開きスクリーンの帯が隠れる／パネルの次へ・キー・一覧で送る／
  パネルに次の受賞者名、スクリーンの予告には出ない／伏せてから名前が出る／2列の配置／枠がはみ出さず発表の文字と重ならない（1920×1080・1280×720）／
  パネルで作業画面へ戻る・再開／閉じると帯が戻り、帯と下の枠が重ならない／帯から開き直す／順位が変わると反映を促し、反映すると始められる／
  **スクリーンを2回読み込み直しても、古いパネルを閉じて開き直せる（ページは2つのまま）・開き直したパネルで送れる**／390px。
  段階3〜6（計140項目）も再実行して NG 0（段階5の検査はボタン名の変更に合わせた）

### デザイン案との比較（1920×1080・1280×800・390×844）

比較画像は受け渡しの zip に同梱（リポジトリには置かない）。

**意図して変えたもの**

| デザイン案 | v51 | 理由 |
|---|---|---|
| 見出し「RANK」 | 「第 3 位」「優　勝」 | 既存の見出し（読み上げやすさと検査） |
| 金額「¥3,000」 | 「3,000円」 | アプリ全体の表記（4.11） |
| 発表の順番「ブービー → … → 優勝 → ベスグロ」（9枚） | 9/8確定の並び | 4.7の表の1行目 |
| サイドバーの「発表スクリーン」 | 「結果・発表」のまま | 結果の入力がある画面のため |
| 「← 2位 佐藤 健一」（前の画面の順位） | 実際の前の画面（例「← ベストグロス 青木 健一」） | 並びが違うため |

**残っている差（後の段階）**：スマホの発表画面の仕上げ（段階8）。

### 公開の確認（2026-09-17、v51）

GitHub の main（v51、プレビューの崩れを直した版）は、照合用の md5 で38ファイルすべて一致、余分なファイルなし、
25本・1950項目・NG 0、`pc.html` に文書の骨組みの札なし、を確認した。

## 4.16 段階8：スマホ（2026-09-17、v52）

760px以下の画面を、デザイン案の M1〜M3 に合わせて作り直しました。**PC（1024px以上）と 761〜1023px は変えていません**（ブラウザで確認）。
**始める前に3点を決めました。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | スマホの上部（ロゴ・保存状態・タブ列で高かった） | **デザインどおり低くする。すべての画面へは大会ハブの「すべての画面」から**（4.10の「上のタブ列も残す」はこの決定で置き換えた） |
| 2 | 1人ずつのスコア入力の数字キー | **画面の数字キー**（端末のキーボードを出さない） |
| 3 | 今回の範囲 | **M1〜M3と上部まで。参加者の表のカード化は段階9へ** |

### 上部（約200px → 64px）

- 大会名（小さく）と「当日 08.28」（段階と開催日、大きく）、段階の切替だけ（`mobHead()`、`#mHead`）
- ロゴ・大会名の見出し・保存状態・PCの見出し・上のタブ列はスマホでは隠す（保存の状態はホームとデータの画面にある）
- **「次にやること」は大会ハブ以外では1行の小さい帯**（`nx-mini`。説明・手順・「◯◯へ進む」を隠す）

### M1：大会ハブ

- 数字のカード・追加ルール・次の段階の流れはスマホでは隠す（集金の見込みと本文の見出しは残す）
- いまの段階のチェックリストを**手順のカード**にする（番号の丸、済んだら✓、いま取りかかる手順は金の枠、下に「集金 0/16」のような画面の名前と進み、右に ›）
- 下に「前の段階の画面は、下の「すべての画面」から開けます」
- **すべての画面**（`hb-all`、スマホだけ）：大会ハブ／準備（畳んでいれば「畳んでいます」）／当日（いま）／発表／そのほか（ホーム・データ・使い方）。進み数つき。
  **畳んでいる段階の画面は、その段階を開いてから移る**（`goAll()`。開かずに移ると見えるタブへ戻されるため）

### M2：1人ずつのスコア入力

スマホでは1人ずつの画面が既定（`SCVIEW="one"`）。「‹ 一覧」で従来の表に、表の上の「1人ずつ入力する」で戻る。

| 置いたもの | 中身 |
|---|---|
| 上 | ‹ 一覧 ／ ‹ 11 / 16 › ／ 次の人 › 、進みの帯 |
| 人 | 「2組・3人目」と名前（大きく） |
| 枠 | 集計表方式：グロス・HDCP・ネット。隠しホール方式：隠しホール12＋グロス（5つずつ）。18ホール方式：18ホール（6つずつ）。押すとその欄へ |
| メモ | 集計表方式は「3つが一致しました」「グロス − HDCP ＝ 77.6 のはず（入れたネットは 8）」（合わなければネットの枠を赤く）。ホール方式はグロス・HDCP・ネット |
| 数字キー | 1〜9・小数点・0・⌫。小数点は HDCP・ネットだけ。`touch-action:manipulation` で二度押しの拡大を止める |
| 次の欄へ | 「次の欄へ（HDCP）」→ 最後の欄は「次の人へ」→ 最後の人は「一覧で確かめる」 |

- **入力欄を使わない**ので、端末のキーボードも日本語入力も出ない（落とし穴1・2とは無関係）
- **押すたびに `scSet` で保存する**（途中で画面を離れても消えない）。欄に移った直後の数字は入っている値と置き換える
- 桁：グロス3桁、ホール2桁、HDCP・ネットは整数3桁と小数1桁。小数点から打つと「0.」。先頭の0は残さない
- 始める人：入っていない最初の人（集計表方式は3つのどれかが空、ホール方式はグロスが出ない人）
- 1人ずつの入力中は、スマホでは本文の見出しと「次にやること」も隠す（`pane.m2mode`。デザイン案M2と同じ、入力だけの画面）
- **「次の欄へ」が下タブの上に収まる**ことを 390×844・430×932・375×667（iPhone SE）・360×640 で、3方式とも確認（背の低い画面では部品を詰める）
- 集計表方式で「○」の記号は使わない（`sheet.js` が○の数を数えているため。「一致」と書く）

### M3：順位のカード

- 表と上の操作はスマホでは隠し、**順位をカードで**並べる（順位・名前・「グロス − HDCP ・ 受賞 ・ 同ネットの理由」・ネット）。1位は金の地
- 「ネット／グロス」の切替（`RKSORT`、ファイルに残さない）
- 「順位確定・ダブルペリア」（反映前なら「反映前」と、合っていない項目の知らせ）
- 下に「▶ 発表をはじめる」（反映前なら「結果を反映して発表の準備へ」）と「結果を共有する」。
  「プロジェクターにつないだPCで開くのがおすすめです」と添える（スマホからも始められる）
- 技能賞・発表の順番・賞金合計の右の列は、スマホでも下に残す

### テスト

- 直したもの：`v2test.js`（上の帯の置き場所にスマホの見出しが入った）
- `v2test.js` に**スマホの検査を68項目**追加（728 → 796）
- **v51 に当てると68項目すべてが NG**、途中で止まらない
- 合計 **2018項目**（1950 ＋ 68）。`pc.html` 約619KB（上限700KB）
- **実際のブラウザ（Chromium、タッチあり）で38項目**：上部が64px・ロゴ等が見えない／すべての画面から畳んだ参加者へ／小さい帯／
  1人ずつ：数字キーで 92・14.4・77.6 が入り「一致」、入力欄にフォーカスが無い、次の人へ、枠を押して移る、「次の欄へ」が下タブの上、連打しても拡大しない、一覧との行き来／
  隠しホールではみ出さない／順位のカード・グロス順・共有へ・発表を始める／スマホの6画面で横にはみ出さない／
  **PC（1280）とタブレット幅（900）は変わらない**（表・上の帯・数字のカード・サイドバー／上のタブ列）
- 段階3〜7（計172項目）も再実行して NG 0。段階7の検査は、スマホ用に同じ名前のボタンが増えたので、PCの上の操作（`.r-bar`）のボタンを押すよう直した（落とし穴36）

### デザイン案との比較（390×844・375×667・1280×800・1920×1080）

比較画像は受け渡しの zip に同梱（リポジトリには置かない）。

**意図して変えたもの**

| デザイン案 | v52 | 理由 |
|---|---|---|
| 下タブ「ハブ・スコア・技能賞・順位」 | 「ハブ」＋いまの段階の画面（当日は集金・スコア・技能賞） | 4.10の構成。順位は発表の段階 |
| 段階の切替「準備・当日」 | 「準備中・当日・発表」 | 3段階（4.7） |
| M2 の「次の人 ›」だけ | 「‹ 11 / 16 ›」で前の人にも戻れる | 打ち直しのため |
| M2 の検算「78.4?」 | ネットの枠を赤く、メモに「◯◯ のはず」 | 1人ずつの画面には検算の列が無いため |
| M3 の「▶ 発表をはじめる（PCで表示）」 | 「▶ 発表をはじめる」＋「PCで開くのがおすすめ」 | スマホからも始められる |

**残っているもの（段階9）**：参加者の表のカード化、使い方の書き直し、印刷、全画面の照合、実機確認。

### 公開の確認（2026-09-17、v52）

GitHub の main（v52）は、照合用の md5 で38ファイルすべて一致、余分なファイルなし、25本・2018項目・NG 0、
`pc.html` に文書の骨組みの札なし、を確認した。

## 4.17 段階9：仕上げ（2026-09-17、v53）

デザイン案の取り込みはこれで完了です。**始める前に3点を決めました。**

| # | 論点 | 決定 |
|---|---|---|
| 1 | 使い方の画面 | **全面的に書き直す**（ホーム → 準備 → 当日 → 発表の流れ、スマホ、操作パネル） |
| 2 | スマホの参加者の表 | **1人1カード**（氏名・所属・生年月日・組。押すと枠や参加のしかたを開く） |
| 3 | 全画面の照合で差が見つかったとき | **小さな差（余白・文言・色）はこの段階で直し、構造の差は一覧にして相談** |

### 使い方の書き直し

- 段階の見出し（ホーム／準備／当日／発表／いつでも）で区切り、**手順は通し番号**。いまの段階に印を付ける
- 画面の名前は `TABLAB()` から引くので、名前を変えても説明とずれない。**使っていない機能の手順は出さず、番号は続けて振る**
- 「画面の選び方」（PCは左の一覧・タブレットは上のタブ列・スマホは下タブと大会ハブの「すべての画面」）、
  スマホでの使い方（参加者のカード・1人ずつのスコア入力・長押しで運ぶ）、**操作パネルとスクリーンの分け方**を追加
- 「困ったときは」に、画面が見当たらない／予想を直したい／順位が変わった／印刷が崩れる、を追加
- 既存の検査（`help.js`）が求める節（作業の流れ・いまのルール設定・貼り付けで入力できるもの・発表画面の操作・保存について・困ったときは）と
  設定に応じて変わる文面（ペリアの式・単価・方式・枠数・対象人数）はそのまま残した

### スマホの参加者（1人1カード）

- 760px以下では表を隠し、**1人1枚のカード**（番号・氏名・所属と生年月日・組／枠／予想のみの印）
- **押した1人だけ**欄が開く（`PEXP`。ファイルには残さない）。開くと氏名・所属・生年月日・組・枠・実力・参加のしかた・原資・個別額・車、上へ／下へ／削除
- **閉じているカードには入力欄を出さない**（表と二重にならない。既存の「欄の数を数える検査」を壊さない）
- 並べ替え・追加・削除をしても、開いているカードが別人にずれない

### 印刷

- スコア入力は入力のための画面なので**印刷では中身を出さず**、「スコアの一覧は『順位・表彰』の画面から印刷できます」と1行だけ出す
- 要約の行・表の行・組のカード・検算のカードを**ページの境目で分割しない**（`break-inside:avoid`）。見出しの直後で改ページしない
- 強調の色（金・赤）は印刷では黒にする
- **公開前の確認で見つけた不備**：集金・集計・結果共有・参加者を印刷すると、**紙に大会名も日付も出ていなかった**。
  PCでは画面の見出しを上の帯に出しており、本文の見出しを隠していたため。**印刷のときだけ本文の見出しを出し、大会名の行（`pg-nm`）を添える**ようにした

### 全画面の照合（デザイン案 ⇔ v53）

同じ画面サイズで撮って並べた（比較画像は受け渡しの zip に同梱）。**構造の差は残っていません。**
下は意図して変えた点だけです（4.7の表の8項目を含む）。

| 画面 | 意図して変えた点（理由の節） |
|---|---|
| P1 ホーム | 右上は「データ・使い方」（デザイン案は「コースマスタ・使い方・利用者アイコン」）。1人で使うアプリなので利用者アイコンは持たない |
| P2 大会作成 基本 | 開催日は1つの欄（西暦・和暦・区切り文字を読み取る）。**「最近使った会場」の札は未実装**（履歴を持っていない） |
| P3 追加ルール | ルールが多いので競技／予想ゲーム／賞とお金に分けた（4.11） |
| P4・P9 大会ハブ | 数字のカードは状況で3枚を切り替える（4.11） |
| P5 参加者 | 列は削らず切替で残す。行の操作は▲▼×（4.12） |
| P6 組合せ | トレイの下に「組から外した方・予想のみの方」（4.14の設計の要点） |
| P7 スコア入力 | 検算の「集計表が正しい」は採らない。Tabはブラウザの標準（4.13・4.7） |
| P8 順位・表彰 | 「発表をはじめる」は反映済みのときだけ。賞は列で出す（4.13） |
| P10 予想入力 | 「締切まで◯分」「集金済みの口数まで」は採らない（4.7・4.14） |
| P11 発表 | 見出しは「第 3 位」、金額は「3,000円」、発表の順番は9/8確定の並び（4.15・4.7） |
| M1〜M3 スマホ | 下タブはハブ＋いまの段階。段階の切替は3つ。M2は前の人にも戻れる（4.16） |
| 共通 | 配色・字体・1大会＝1ファイルは第1版の方針どおり（4.7） |

### テスト

- `v2test.js` に**仕上げの検査を48項目**追加（796 → 844）。使い方の節と番号・スマホの参加者カード・印刷の指定・空白の画面が無いこと・全40画面が描けること
- 印刷の不備を直したぶん**2項目**追加（合計 **2068項目**、2018 ＋ 50）
- **v52 に当てると48項目のうち42項目が NG**、途中で止まらない
- **実際のブラウザ（Chromium）で38項目**：スマホの参加者カード（開く・直す・入れ替わる・閉じる・押しやすい高さ）／
  大会ハブでは見出しを出さない／使い方が読める／**印刷の表示で6画面に中身と大会名・日付が入り、操作の部品が出ない**／PCは表のまま。
  段階3〜8（計210項目）も再実行して NG 0
- `pc.html` 約631KB（上限700KB）

### 残っている宿題

1. **実機確認**（`CompeMasterPro_実機確認シート_v53.md` を同梱）。iPhone・本番の機材でしか確かめられない27項目
2. P2 の「最近使った会場」（履歴を持つかどうかは未決）
3. git履歴に残る実名の会場（4.7の宿題のまま）

## 4.18 スコア入力の導線（2026-09-18、v54）

**きっかけ**：「一人ずつ全部のホールがそろわなくても成立するはず」という指摘。実測したところ、成立はするが**気づけない**作りだった。

| 方式 | ホールが1つ欠けたとき（v53まで） |
|---|---|
| 隠しホール＋グロス | ハンディが出ず**その人が順位表から静かに消える**のに、「スコアを入れる」は**済みのまま**（グロスだけを見ていたため） |
| ゴルフ場の集計表 | HDCPが空でも同じく済みのまま |
| 18ホールすべて | グロスも出ないので「残り1名」と出る（ここだけ正しく気づけた） |

**決めたこと（3点）**

| # | 論点 | 決定 |
|---|---|---|
| 1 | 「スコアを入れる」の完了判定 | **ネットが出た人数で判定**し、欠けている人を名前と理由で出す |
| 2 | 隠しホールが分からない人への逃げ道 | **その人だけハンディを直接入れられる**（18ホール方式ではグロスも） |
| 3 | 入力しないのが正しい人 | **「棄権・ノーカード」の印**を用意し、入力漏れと区別する |

### データ（項目の追加だけ。古いファイルはそのまま開ける）

- `scores[氏名].hcOver`：直接入れたハンディ。**本来のハンディとして扱い**、上限（`hcMax`）の丸めと「上限に達した方」の知らせは計算のときと同じ
- `scores[氏名].nc`：棄権・ノーカードの印
- 18ホール方式のグロスは既存の `gross` を使う（ホールがそろえば合計、そろわなければ直接入れた値）

### 画面

| 場所 | 変えたこと |
|---|---|
| スコア入力の上 | 進みを**ネットが出た人数**で数える。「ノーカード ◯名」も添える |
| スコア入力の知らせ | **順位に入っていない方を名前と理由で出す**（「◯◯（ハンディが未確定）」「◯◯（グロスが未入力）」）と、直し方 |
| 表 | HDCPの欄を**直接入力**に（空欄なら計算値を薄く下敷きに。入れると金色）。18ホール方式はグロスも直接入力。**ノーカードの列**（チェック）。ノーカードの行は薄く、欄は押せない |
| スマホの1人ずつ | 「ハンディを直接」の欄と「棄権・ノーカードにする」。ノーカードにすると数字キーが止まり、ボタンは「次の人へ」 |
| 順位・表彰 | 「棄権・ノーカード ◯名（…）は順位に入りません」「まだ順位に入っていない方が ◯名」 |
| 使い方 | 「棄権・ノーカード」は空欄ではなく**印**で。「ハンディが出せないとき」の節を追加 |

**ノーカードの印を付けた方**は、順位・GTOの下位3名・罰金のどれからも外れ、完了判定では「済み」に数える（入れないのが正しいため）。
グロスを空欄にする従来のやり方も動くが、印を付けたほうが入力漏れと区別できる。

### ホームへ戻る道（同じ版で直した）

**きっかけ**：「ホームに戻る手段が分かりにくい。必要か判断」という指摘。

**判断：ホームは必要**。ホームにしかない機能が3つある（新しい大会・**前回からコピー**・ファイルを開く）。
データの画面にあるのは書き出し・サンプル・全消去だけ。ただし**当日の運営中は一度も使わない**ので、下タブの枠は使わない。

実測した道：PCは2つ（サイドバーのロゴ／足元の「ホーム」）、タブレット幅は1つ（上のタブ列）、
**スマホは「大会ハブ →すべての画面→ ホーム」の1つだけで、ページを1画面ぶん（約1200px）送る必要があった**。

直したこと（9/18の決定）：

- **スマホの上部の大会名を押すとホーム**（家の印 ⌂ を添えた。PCのロゴと同じ意味。画面の上端にあるので送らずに押せる）
- **大会ハブの足元に「⌂ ホーム／データ／使い方」を常設**（PCのサイドバー足元と同じ並び）。
  「すべての画面」の中の「そのほか」は外し、**1か所だけ**にした（二重にしない。落とし穴16）
- PC・タブレット幅は変えない

### テスト

- `v2test.js` に**35項目**追加（844 → 879）、ホームへ戻る道で**4項目**（→ 883）。`privacy.js` に日付の理由を1件、`void.js` の使い方の節名を更新
- **v53 に当てると35項目のうち32項目が NG**、途中で止まらない
- 合計 **2107項目**（2068 ＋ 35 ＋ 4）。`pc.html` 約638KB（上限700KB）
- **実際のブラウザ（Chromium）で25項目**：欠けると名前と理由で知らせる／ハンディを直接入れると順位に戻り知らせが消える／
  空に戻すと再び知らせる／ノーカードで済みになり行が薄くなる・欄が押せない・順位から外れる／18ホール方式のグロスの直接入力／
  スマホの直接入力とノーカードのボタン／**上部の大会名と大会ハブの足元からホームへ戻れる（PCはロゴと足元のまま）**。
  段階3〜9（計248項目）も再実行して NG 0

## 4.19 集金のあとの差額（2026-09-18、v55）

**きっかけ**：9月16日時点の積み残し「集金したあとに前提が変わったときの差額管理」。実測で、同じ型の不具合（落とし穴39）だと分かった。

集金の記録は「受け取ったか（はい／いいえ）」だけで、**いくら受け取ったかを持っていなかった**。
サンプル①に頭割りのガソリン代37,000円・500円単位の切り上げを入れ、全員を受け取り済みにしてから1名を組から外すと、
欠席者は −24,000円、3名は +500円に変わるのに、**画面は「16/16 受け取り済」のまま**だった。

**決めたこと（4点。推奨案どおり。3だけ絞った）**

| # | 論点 | 決定 |
|---|---|---|
| 1 | 記録するもの | **受け取った額と、そのときの内訳**（実費・原資・口数・キャンセル料・頭割りの人数）。旧版の「受け取り済」は、開いた時点の徴収額を受け取ったものとみなす |
| 2 | 済みの判定 | **差額が0の人だけ済み**。差額のある人は名前・金額・理由で出す（大会ハブ・集金の画面・サイドバーの数） |
| 3 | 精算のしかた | 既定は1人ずつ。**追加でいただく差額だけ**、ボタン1つで繰越金で埋められる。**お返しする差額は本人のお金なので埋めない**（一括の対象にすると、欠席者への返金まで繰越金に吸われる） |
| 4 | 欠席者のキャンセル料 | プレーしない方（実費の対象外）に**キャンセル料の欄**（既定0円）。本人の徴収額に足し、収支は「キャンセル料の預かり」＝「ゴルフ場へのキャンセル料」 |

### データ（項目の追加だけ。古いファイルはそのまま開ける）

- `paid[氏名]`：`true` から `{y, w, play, fee, kq, gq, cancel, mem}` に。`y` は受け取った額（お返ししたぶんは引く）、`w` は繰越金で埋めた額
- 埋めた額は**いま足りないぶんまで**しか数えない。あとで徴収額が下がる（欠席者が戻るなど）と、埋めたぶんから先に消える
- `players[].cancel`：キャンセル料。プレーしない方（`fee===false`）にだけ効く。組に戻ると自動で効かなくなる

### 画面

| 場所 | 変えたこと |
|---|---|
| 集金の上 | 「受け取ったあとで金額が変わった方が ◯名」と、1人ずつ**金額と理由**（「実費 +154円（頭割りの人数 16→15名）」「プレーしないため、実費と賞金の原資がなくなりました」）。「追加でいただく ◯名・◯円を繰越金で埋める」 |
| 集金の表（PC） | 差額があるときだけ「受け取った額」「差額」の列。差額の行は済の欄が **△**（チェックを外す操作にしない。記録ごと消えるため）。差額の欄のボタンで精算 |
| スマホのカード | 差額のカードは**点線の枠・△・`aria-checked="mixed"`**。追加は黄、お返しは緑。「受け取り済 ◯円 → いま ◯円」と理由。押すと精算。「未集金だけ」にも残る。下の帯に「差額 ◯名」 |
| キャンセル料 | 集金の画面に畳んだ欄（入力があれば開く）。表に「キャンセル料」の列 |
| 大会ハブ・サイドバー | 「集金する — 差額4名」と名前・金額。サイドバーの数も差額を除いて数える |
| 収支 | 「キャンセル料の預かり」「ゴルフ場へのキャンセル料」「差額の穴埋め（繰越金から）」 |
| 使い方 | 困ったときはに「集金のあとで欠席が出た」。スマホの集金に △ のカード |

### あわせて直した不具合（以前からあったもの）

1. **スコア入力のサイドバーの数がグロスで数えていた**（v54の取り残し）。ホールを1つ欠くと、大会ハブは「残り1名」なのにサイドバーは「16/16」。
   `scoreState()`（ネットが出た人数、ノーカードは済み）にそろえた。「順位を確かめる」の「未入力◯名」も同じ数え方の「未確定◯名」に
2. **集金表の見出しの段が列とずれていた**。「個別」「精算」の見出しの幅が列の数と合わず、罰金・お渡しの上が空いていた
3. **集金表を印刷すると、徴収額より右（賞金・払戻・罰金・お渡し）が紙から切れていた**。画面用の横スクロール（`hscroll`）のまま紙に出していたため。
   紙では表として組み、列の最小幅を外して9pxに詰めた。列が最も多い場合（差額＋キャンセル料、19列）でもA4縦1枚に収まる。
   カードごと次のページへ送られて1枚目が空いていたので、集金のカードだけはページをまたいでよいことにした（行は分けない）。金額の色は紙では黒
4. 下の帯の分母に、徴収額0円の方（何も集めない方）を数えていた。「残り1名 0円」が消えずに残る形だった

### テスト

- `v2test.js` に **53項目**追加（885 → 938）。合計 **2160項目**（2107 ＋ 53）
- **v54 に当てると53項目のうち50項目が NG**、途中で止まらない（通る3項目は「差額が無いときは従来どおり」の確認）
- **実際のブラウザ（Chromium）で35項目**：PC（差額の知らせ・4つのボタン・+500を押す・繰越で埋める・お返しを押す・サイドバーの数・キャンセル料の入力）／
  印刷（知らせと欄は出さない・差額の列は出る）／スマホ 390×844・375×667・360×640（差額のカード・横にはみ出さない・押しやすい高さ・押すと減る・下の帯・文字が切れない）。
  あわせて、A4の幅で集金表が収まることを3つの場合で測った（672px／内側672px）
- `pc.html` 約656KB（上限700KB。残り約44KB）

### 実機で確かめること（実機確認シート v55 に追加）

- iPhone：差額のカード（点線・△）が押せて、押すと消える。下の帯の「差額 ◯名」
- PC：「繰越金で埋める」の確認が出る。キャンセル料を入れると差額が出る
- 印刷：集金表が**A4縦1枚に右端まで**入る（お渡しの列まで）

## 4.20 背の低い画面のサイドバー（2026-09-18、v56）

> **v57で置き換えました（4.21）。** 見た目の対処では原因（左右を別々にスクロールさせる作り）が残るため、1本のスクロールにしました。下は経緯として残します。

**きっかけ**：実機（MacBook の Chrome、本文の高さ約660px）の画面で「フレームが残っている」と指摘。
準備の段階で画面が多いと、サイドバーが画面の高さを超えてスクロールになる。そのとき3つ起きていた。

1. **ブラウザ標準の明るいスクロールバーが、サイドバーと本文の間に白い枠のように出ていた**
   （macOS で「スクロールバーを常に表示」やマウス接続時、Windows。トラックパッドだけの Mac では出ないので気づきにくい）
2. **足元の「ホーム・データ・使い方」が画面の外へ押し出されていた**（4.18で直したホームへ戻る道が、背の低い画面で切れていた）
3. スクロールバーの幅（16px）だけ狭くなり、「ニアピン・ドラコン 0/8」が2行に折れていた

**直したこと**

- サイドバーのスクロールバーを**細く地の色**に（`scrollbar-width:thin`・`scrollbar-color`。Safari は `::-webkit-scrollbar`）
- 足元を**画面の下に貼り付け**（`position:sticky`）。上に地の色のぼかしを敷き、送った項目が足元の下に潜って見えるようにした
- 背の低い画面（高さ820px以下）では、**マウスで使うときだけ**行を詰める（38px → 32px）。タブレットの指には狭くしない
- 本文の右端や表の横スクロールのバーも地の色に（`scrollbar-color` は子へ受け継がれるので、`html` の1か所で全体にかかる）。
  スクロールバーを隠している上のタブ列はそのまま

**テスト**：`v2test.js` に7項目（938 → 945）。合計 **2167項目**。v55 に当てると7項目のうち5項目が NG（通る2項目は「変えていないもの」の確認）。
**実際のブラウザで14項目**（スクロールバーを出した状態で、1200×660・1280×1000・タブレット幅）。
自動テストの Chromium は標準でスクロールバーを隠しているので、**`--hide-scrollbars` を外して**再現した（落とし穴44）。
v55の集金の検査35項目と、印刷の幅の測定も再実行して変わりなし。`pc.html` 約658KB（上限700KB）

## 4.21 1本のスクロール（2026-09-18、v57）

**きっかけ**：「ヘッダーの固定や左右の分離は必要か。深くスクロールしても使いにくいとは感じない」との問い。実測して判断した（1200×660）。

| 実測 | 値 |
|---|---|
| PCの固定ヘッダー | 73px（画面の11%）。中身は画面名・日付・保存・段階の切替で、下へ送った状態で使うものがない |
| タブレット幅の固定ヘッダー | **226px（画面の3割）**。ロゴ・段階の切替・タブ3段 |
| サイドバーが画面を超えるのは | 準備の段階だけ（746px > 660px）。当日・発表は収まる |
| 画面の深さ | 主な作業画面は1.7〜2.7画面分。長いのは予想入力3.6・結果発表3.6・賞金収支5.0・使い方7.9 |

**決めたこと（案B）**：PCとタブレット（761px以上）は、**サイドバーも上の帯も本文と一緒に流す1本のスクロール**にする。
スマホ（760px以下）は、低い上の帯（大会名の ⌂ でホームへ）と下タブを**固定のまま**（4.18の決定）。
白い枠・足元が隠れる・項目が折れる、の3つは「左右を別々にスクロールさせる作り」から出ていたので、v56の見た目の対処は外した。

**デザイン案からの構造の変更**です（デザイン案はサイドバー固定）。4.17の「意図して変えた点」に次の1行を加える扱い：
「共通：サイドバーと上の帯はPC・タブレットで本文と一緒に流す（4.21）」。

**あわせて直したもの**：タブレット幅の上の帯に、**旧デザインのロゴ（紋章と英字の副題）が残っていた**（実機の画面で指摘）。
PCではサイドバー、ホームでは文字のロゴ「CompeMaster PRO」に変わっていたが、タブレット幅だけ取り残されていた。
同じ文字のロゴにそろえ、**押すとホームへ**戻るようにした（PCのサイドバーのロゴ・スマホの ⌂ と同じ意味）。

**変えたところ**

- `.side`：`position:sticky; height:100vh; overflow-y:auto` → `align-self:stretch; min-height:100vh`（地の色を本文の高さまで伸ばす）
- 足元（ホーム・データ・使い方）は一覧のすぐ下（`margin-top:18px`）
- PCでは下の余白90px（スマホの下タブのぶん）を本文の側へ移し、サイドバーの地の色をページの下端まで届かせた
- `#work>.top` は761px以上で固定しない。スマホは固定のまま
- v56の足元の貼り付け・細いスクロールバー・行の詰めは削除。本文と表のスクロールバーを地の色にする指定（v56）は残す

**テスト**：`v2test.js` のv56の7項目を、v57の10項目に置き換えた（945 → 948）。合計 **2170項目**。
v56 に当てると10項目のうち7項目が NG（通る3項目は「変えていないもの」の確認）。
**実際のブラウザで23項目**（スクロールバーを出した状態で、PC 1200×660・1280×800・1920×1080、タブレット 830×787、スマホ 390×844・375×667）：
サイドバーにスクロールバーが出ない／上の帯が流れる／地の色が下端まで／送ると一覧の最後と足元が見える／一覧と足元とロゴで画面を移れる／スマホの上の帯は固定のまま／横にはみ出さない。
v55の集金の検査35項目と印刷の幅も再実行して変わりなし。`pc.html` 約657KB（上限700KB）

## 4.22 PDFの宿題の取り下げ・読めない開催日・作り直しの完了（2026-09-18、v58）

### ゴルフ場にPDFを頼む宿題を取り下げた

**平松さんの判断（9/18）**：「ゴルフ場に集計表のPDFを頼む」は、**文字認識（OCR）を検討していた頃の名残**で、必要ない。
スコア入力の正規手順は**打ち込んで1行ずつ検算すること**だけ（3章）。全行○で正しいと言い切れるので、別の形式に頼る理由がない。

- アプリの貼り付け欄と使い方から「**PDFがいちばん確実です**」「**PDFでもらえるなら**」を外し、「正規の手順は打ち込んで検算」「ゴルフ場に別の形式を頼む必要はない」に書き換えた
- `docs/README.md` の「集計表の取り込み」も同じく書き換え（方法を3つ並べるのをやめ、正規手順1つ＋貼り付けは補助）
- **貼り付けの機能（①②③の読み方）は残した**。手元にExcelなどの文字があれば使える。写真の文字は当てにしない位置づけのまま
- `pwa.js` の「READMEにPDFが確実と書いてあること」の検査を、「PDFを頼む必要はないと書いてあること」に置き換えた

**同じ提案を繰り返さないこと。** 「PDFをもらえば速い」は、打ち込みの検算で足りているので価値がない。

### 読めない開催日を知らせる

大会設定の開催日は、「2027.920」のような入力が**黙って通り**、曜日も「開催まで◯日」も出ず、大会ハブの「大会の基本を入れる」は**済み**になっていた
（作成画面には知らせがあったが、大会設定には無かった）。実機の画面で見つけた。

- 大会設定の開催日の下に「日付として読めません。例：2027.9.20」（欄を離れたとき。1文字ごとには描き直さない。落とし穴1）
- 「大会の基本を入れる」は、**開催日が日付として読めるまで済みにしない**。「— 開催日が読めません」と理由を出す（落とし穴39と同じ考え方）

### リポジトリの作り直しを完了した

9月18日16:37。コミット2件（v56の7ファイル → v57の全38ファイル）。**履歴の実名・実在の生年月日は0件**。9章を参照。
1回目に誤って「v56更新分」（7ファイル）を上げたため2件になったが、どちらにも実名は入っていない。**同じ名前のフォルダで展開されるzipは取り違えやすい**（落とし穴47）。

### 実機確認（9/18、平松さん）

**確認済み**：12a・12b（iPhoneの集金の差額カード・キャンセル料）、18b（集金表の印刷がA4縦1枚に右端まで）、21〜27（本番の機材：操作パネル・全画面・送り・次の受賞者名・音・2列の読みやすさ・読み込み直し）。
v57の公開後に、公開ページ・1本のスクロール・スマホのアイコン入れ直しも確認済み。

### テスト

`v2test.js` に9項目（948 → 957）。合計 **2179項目**。v57に当てると9項目のうち6項目が NG（通る3項目は「読める日付なら済み」「貼り付けの機能は残す」「直せば済み」の、前の版でも正しい動き）。
実際のブラウザで4項目（大会設定で打って欄を離れると知らせ・次にやること・直すと消える・例外なし）。v57の23項目・v55の35項目も再実行して変わりなし。`pc.html` 約658KB（上限700KB）

## 5. 設計上の決めごと（変えるときは理由を確認すること）

| 決めごと | 理由 |
|---|---|
| **HTMLファイル1つ。外部ライブラリもCDNもゼロ** | ゴルフ場は電波が弱い。USBで持ち運べることが最優先 |
| **文字認識（OCR）は積まない** | 5〜20MBになり単一ファイルが壊れる。精度も端末標準に及ばない（9月4日に再確認） |
| **スコアは打ち込んで検算する** | 「正しいと確認できること」が安心の中身。認識精度を上げることではない |
| **自動保存はlocalStorage、ただし端末ごと** | 保存の主役はファイル書き出し。自動保存は事故防止の保険 |
| **画面を作り直す処理は入力確定時だけ** | 1文字ごとに作り直すと日本語変換が壊れる（過去に2度発生） |
| **ゴルフ場マスタは選ぶだけ、手入力の道も残す** | 収録外のコースでも使えること |
| **配布物に実名を入れない** | 公開リポジトリに実名が載る事故が9月4日に発生。`privacy.js` で検査 |

### 過去に踏んだ落とし穴

1. **`oninput` で `render()` を呼ぶと日本語が打てない** — 確定時（`onchange`／`compositionend`）に変更
2. **Enterでの候補選択が、変換確定のEnterを拾う** — 機能ごと撤去
3. **施設を切り替えたとき9Hの番号が範囲外のまま残る** — `mFix()` で毎回補正
4. **`normalize("NFKC")` が氏名の全角スペースを半角にする** — 一時退避してから正規化
5. **Google Fontsの読み込みでオフラインが壊れる** — 端末内の明朝体に置換
6. **集計表の解析がOUTの小計をグロスと誤認**（9/4）— 先頭から40〜200の数値を拾っていたため、
   `42`（OUTの小計）をグロスとし、HDCPが負になっても○判定で通っていた。**末尾から検算する方式に変更**
7. **CSSの後勝ちの上書きに気づかず修正が効かない**（9/4）— `.top{padding-top:11px}` が
   ファイル末尾にあり、セーフエリアの指定を打ち消していた。**テストは「最後に勝つ値」を見るように強化**
8. **表の列に幅の指定がないと1文字ぶんまで潰れる**（9/4）— 氏名欄が潰れ「佐々木」が「佐々オ」に。
   **文字の入る列すべてに下限幅を与え、`resp.js` で指定漏れを検査**
9. **「取れた数が多いほう」で判断すると誤った結果を採る**（9/4）— 数語ごとに改行が入ると
   行ごとの解析が16行取れたつもりで誤る。**検算を通った側を優先する**ように変更
10. **音が一度も鳴っていなかった**（9/4）— `MASTER=AC.createGain()` の書き間違い。
    `MASTER` はゴルフ場マスタの `const` 配列で、代入で例外になっていた。
    `draw()` がそれを受け止めていなかったため進捗表示も止まっていた。
    **テストが `AudioContext` を投げる器で動かしており、音の経路を一度も通っていなかった**のが
    気づけなかった理由
11. **`width:` は縮む**（9/8）— 表が幅100%に押し込まれると比例して圧縮される。
    文字の入る列は `min-width:` を使う
12. **容量を文字数で測っていた**（9/8）— 日本語は1文字3バイト。294KBと報告したが実際は398KBだった。
    `mst.js` はバイト数で測る
13. **Pagesの公開元が `/(root)` に戻る**（9/8）— 非公開化して再公開すると `/docs` の設定が外れる。
    トップが404になり、`404.html` も効かなくなる
14. **手元のコピーから作業を始めると他の作業が消える**（9/8）— 別アカウントの変更に気づかず
    上書きしかけた。**作業前に必ず `git clone`**
15. **同じカードを2回呼ぶ**（9/16）— 収支タブで `collectCard()` を2箇所から呼び、
    同じ表が丸ごと2つ並んでいた。`resp.js` が呼び出し回数を検査する
16. **入力する画面と結果を見る画面で同じ表を出す**（9/16）— 役割で分ける。
    大会設定は入力表＋小計、賞金・収支は明細のみ
17. **ボタンを消してカードの枠が残る**（9/8）— 読み取りアプリを撤去したとき、
    データタブの「スマホ・タブレットで読み取ったデータ」の見出しと説明文が残り、
    中身が空のカードが画面に出ていた。**関数を消すだけでなく、文言と枠も消す。**
    `pwa.js` が残り文言と空の操作欄を検査する

18. **テストが途中で例外になっても一覧では気づけない**（9/16）— `money.js` が83行目で止まり、
    「合計 NG」が出ないまま9月8日から検査の半分が走っていなかった。**件数（OK の合計）で確かめる**
19. **判定を「済むまで先へ進めない」にすると当日止まる**（9/16）— 欠場・ノーカード・該当者なしは空欄が正しい。
    「次にやること」には**必ず次の段階へ進むボタン**を並べる
20. **既存のクラス名とぶつかる**（9/16）— `.rule` は表彰式の飾り罫だった。新しいクラスを足す前に `grep` する
21. **`<details>` の `toggle` は描画時の open にも届く**（9/16）— 描画した状態と同じ通知は記録しない
22. **スマホでは `label` からチェック欄への転送が効かないことがある**（9/17）— 集金カードの名前や金額を押しても入らなかった。
    押す場所には `button` を使い、処理を直接付ける。**自動テストの Chromium では再現しないので、実機で確かめる**
23. **grid の中で `margin:0 auto` を使うと幅が縮む**（9/17）— サイドバーの横の本文が、内容の少ない画面で中央に細く寄った。
    grid の項目は左右が `auto` だと中身の幅になる。`width:100%` を併せて書く
24. **テストが古い文言を守っていた**（9/17）— `sel.js` が「379施設・867コース」を検査しており、
    全国版にしたあとも案内文の誤りが残った。**数や名前を検査するときは、実物から数えた値と比べる**
25. **文字だけで探すテストは、新しく足した表示を拾う**（9/17）— `typing.js` が最初の「ニアピン」を探しており、
    大会設定に足した札を拾って NG になった。**入力欄は `<label>` や id から探す**
26. **意味の違う状態がデータの上で同じになっている**（9/17）— 組から外すと原資も外れるため、組から外した方と
    予想のみの方が区別できず、トレイへ運んだ人が画面から消えた。**決まりを変えずに、見せる側で両方を出す**
27. **1列に畳むグリッドを `1fr` にすると、表の最小幅まで広がる**（9/17）— スマホで参加者の画面が627pxになった。`minmax(0,1fr)` を使う
28. **ブラウザ検査で、押したつもりの場所を押していない**（9/17）— 上に貼り付いた帯の下に隠れた要素や、同じ文字の説明文を押していた。
    **ボタンは `button:has-text()` で指定し、押す位置は貼り付いた帯の下で取る**。通らないときは、まずテストの押し方を疑う
29. **正規表現の途中の `|` は式全体を2つに割る**（9/17）— `/カードの中[\s\S]*A|B/` は「カードの中のA」か「どこかのB」になり、
    前の版でも通っていた。**前の版に当てて落ちることを確かめる**手順で見つかった。まとまりは `(?:A|B)` で囲む
30. **行の背景にグラデーションを掛けると、セルごとに繰り返す**（9/17）— 1位の行が縞になった。行の色は単色にする
31. **サンプルの状態を変えると、サンプルを使う検査が一斉に動く**（9/17）— サンプル①を「予想を締め切った状態」にしたら、
    予想を足す検査が止められた。**サンプルは「本番の稽古」として正しい状態に保ち、検査の側で必要な前提（解除など）を明示する**
32. **別ウィンドウとのやり取りは「空のページに書き込む」が確実**（9/17）— ファイルを直接開いたときは、
    ページごとに出どころが分かれ、BroadcastChannel などが届かないことがある。空のページは開いた側と同じ出どころになる
33. **ボタンの名前を変えると、ブラウザ検査が止まる**（9/17）— 段階5の検査が「▶ 発表の画面へ」を押そうとして30秒待った。
    名前を変えたら、`grep` で検査の側も探す
34. **スクリプトの中に文書の骨組みの札を書くと、道具が取り違える**（9/17）— `pc.html` は html・head・body の札を省いた書き方。
    スクリプトの文字列にある `<body>` を、Claude のプレビューが本文の始まりと取り違えた。ブラウザでは動くので、ブラウザの検査では気づけない。
    **別ウィンドウの中身は DOM で差し込む**。`design.js` が検査する
35. **ファイルを直接開いたときは、読み込み直すと別ウィンドウに触れなくなる**（9/17）— 出どころが読み込みのたびに変わるため。
    触れるかを確かめてから使い、触れなければ閉じて開き直す
36. **PC用とスマホ用に同じ名前のボタンがあると、ブラウザ検査が見えない方を押す**（9/17）— 段階8で順位のカードに
    「結果を反映して発表の準備へ」を足したら、段階7の検査が見えないスマホ用のボタンを押そうとして止まった。**置き場所（`.r-bar` など）で絞る**
37. **スマホの画面は背の低い機種でも測る**（9/17）— 390×844 で収まっても、iPhone SE（375×667）や 360×640 では
    「次の欄へ」が下タブに隠れた。高さの条件（`max-height`）で詰める
38. **印刷は画面ごとに紙で確かめる**（9/17）— PCでは見出しを上の帯に出して本文では隠しているため、
    集金・集計・結果共有・参加者を印刷すると**紙に大会名も日付も残らなかった**。画面の検査では気づけない。
    印刷のときだけ本文の見出しを出す
39. **「済み」の判定は、最後に使う値で決める**（9/18）— スコアの完了判定をグロスだけで見ていたため、
    ハンディが欠けた人が順位表から静かに消えても「済み」と出ていた。**順位に入る条件（ネットが出ること）で判定する**
40. **同じ判定をしている場所は1つ直すと他が残る**（9/18、v55）— v54でスコアの「済み」をネットで判定するよう直したが、
    サイドバーの数と「順位を確かめる」はグロスのままだった。お金でも「受け取ったか」だけで済みにしていた。
    **判定を直したら、同じものを数えている場所を `grep` で探し、同じ関数（`scoreState()`・`pend`）から数える**
41. **印刷は画面用の横スクロールのまま紙に出る**（9/18、v55）— `hscroll` の表は紙の幅で切れ、右の列が消える。
    v53の確認は「大会名と日付が入るか」だけで、**表の右端まで入るかを見ていなかった**。列の多い表は、紙に当てて右端の列まで見る
42. **`every` は空の配列で真になる**（9/18、v55）— 「3名とも理由が正しい」の検査が、差額が1件も出ない旧版でも通った。
    **件数を先に確かめてから `every`**。前の版に当てて通ってしまう項目は、中身を疑う
43. **正規表現 `<th[^>]*>` は `<thead>` にも当たる**（9/18、v55）— 見出しの列を数える検査が1つ多く数えた。`<th(?:\s[^>]*)?>` で区切る
44. **自動テストのブラウザはスクロールバーを隠している**（9/18、v56）— Playwright の Chromium は標準で `--hide-scrollbars` が付く。
    そのため段階2〜9のどの比較画像にもスクロールバーが写らず、「白い枠」に気づけなかった。
    **スクロールバーの見え方を確かめるときは `ignore_default_args=["--hide-scrollbars"]` で起動し、背の低い画面（1200×660 など）でも撮る**
45. **見た目の対処で済ませると、原因の作りが残る**（9/18、v57）— v56でスクロールバーの色と足元の貼り付けで「白い枠」を抑えたが、
    左右を別々にスクロールさせる作りが原因だった。**不具合が「作り」から出ていないかを先に問う**。使う人の実感（深いスクロールは苦にならない）が判断材料になった
46. **`<style>` は3つある**（9/18、v57）— `h.indexOf('</style>')` で切ると最初の小さな `<style>` だけを見て、本文の見た目の指定を拾えない。
    検査では **全部の `<style>` をつなげて見る**。また、検査の関数に真偽値をそのまま渡すと例外になり全部 NG になる（関数か値かを見分ける）
47. **同じ名前のフォルダに展開されるzipは取り違える**（9/18、v58）— 作り直しで「v56更新分」（7ファイル）を「作り直し一式」と取り違えて上げた。
    どちらも `CompeMasterPro` に展開されるため。**上げる前に一覧の数（38）を確かめる手順を必ず入れる**。照合で数と版を見れば必ず気づける


---

## 6. 予想ゲームのルール（確定分）

### 馬券

- **ネット順位の上位2名が入る枠**を当てる。1口500円（2026年は200円）
- **ゾロ目（同じ枠を2つ）も的中として成立**する
- 自分自身・自分の枠への投票は**可**
- 無くなった枠を指名した口は**無効・返金**

### GTO

- **グロス下位3名**を当てる。3連複か3連単かは**当日、会長が決定**（ハードコードしない）
- 的中者ゼロなら**全額返金**
- 同じ人を2回以上指名した口は入力時に弾く。すり抜けたら**無効・返金**
- 出走しない方を指名した口も**無効・返金**

### 無効になった口の扱い（9月4日に確定）

**売上に入れず、掛金をそのまま返します。得票数にも数えません。**

得票を数えないのが要点です。従来は「欠席者を含む口」の残り2名分の票が生きたままで、
選抜者の顔ぶれが変わり得ました。**2026年は3位を1票差で争っている**ので実害が出る規模です。

無効の口は「集計」タブに一覧で出ます。当日は集金の担当者と突き合わせてください。

### 罰金

- GTOで得票の多い上位3名（**選抜者**）にグロスで負けた人が、負けた人数 × 1,000円
- 得票数が並んだときは「賞金・収支」タブの**手動指定**で会長が決める
- 得票は**有効な口だけ**を数える

### 失格・ノーカード

**グロスを空欄のままにします。** 順位・GTOの下位3名・罰金のいずれからも外れます。
最下位として扱う運用にはしていません（下手を当てるゲームの趣旨から外れるため）。

### 技能賞の対象ホール

ニアピンは**パー3**、ドラコンは**パー5**。何でもニアピンは打数を問わないので制限なし。

**アプリの初期値（ニアピン 4・7・12・16／ドラコン 5・14）は（実在のコース）に合っていません。**
7番・12番・5番はパー4、16番はパー5です。噛み合わないと**警告が出て、ボタン1つで直せます。**

（実在のコース）の場合：ニアピン **2・4・11・15**／ドラコン **3・8・13・16**（うち2つを使う）

---

## 7. 検証スイート

`CompeMasterPro_検証スイート_1ファイル版.md`（ナレッジ内）。**25本・2179項目**が全合格した状態です。

Claudeに「検証スイートを展開して実行して」と伝えれば、MDからファイルを復元して走らせます。
テストと同じ場所に `site/` を作り、配布物10ファイル（`404.html` を忘れずに）を入れる必要があります。`404.html` が無いと `pwa.js` が3件 NG になります。
**展開した構成では 2148項目**になります（`pwa.js` 82・`privacy.js` 20）。この2本はリポジトリの文書や
テスト自身も見るので、配布物だけの構成ではその分を飛ばします。リポジトリでは2179項目です。

### 要点となる5本

| ファイル | 何を保証するか |
|---|---|
| `sheet.js`（132） | 打ち込みの検算、氏名なしの貼り付け、3通りの読み方、写真からの実データ |
| `rules.js`（102） | 技能賞の架空データ、対象ホールとパーの噛み合わせ警告 |
| `haiya.js`（62） | **（実在のコース）の当日集計表で16名の全項目が公式値と一致すること** |
| `void.js`（61） | 無効口の返金と得票除外 |
| `privacy.js`（25） | **配布ファイルに実名・実在の生年月日・組織名が混ざっていないこと。リポジトリ全体で大会と会場が結び付いていないこと** |

**変更を加えたら必ず全部走らせてください。** 過去の不具合はすべて再現テストとして残してあります。

**件数は `OK` の行を数えたものです。** 1本が途中で例外になると、その先の `OK` が減るだけで
「合計 NG」は出ません（9/16に `money.js` で実際に起きた）。**合計が2179に届かないときは、
減った本を探して例外を確かめてください。**

> ナレッジの1ファイル版は9/16に作り直しました。**それでも正本は GitHub の `tests/`** です。

---

## 8. 2026年の実績（参照用・実データ再現済み）

2026年の大会・17名（出走16＋投票のみ1）。パー72（OUT36／IN36）。
隠しホールは `1 2 3 6 7 9 10 12 13 15 17 18` の12ホール（ダブルペリア）。

| | 結果 |
|---|---|
| 馬券 | 95口・19,000円／当選枠 `2-4`・的中26口・**700円／口** |
| GTO | 34口・17,000円／**3連単・的中ゼロ → 全額返金** |
| 罰金 | 5,000円（大隈3,000・相沢1,000・木南1,000） |
| 選抜者 | 甲斐25票・瀬川21票・郷司17票 |

**3連複だったら4口が的中していました**（1口4,200円）。方式の選択で結果が正反対になった年です。
方式は前夜の2次会、投票を締め切る場で会長が決定します。

### 実データ再現の結果（9月4日に完了）

当日の集計表（`IMG_8530.png`）を投入し、**16名すべてのOUT・IN・グロス・HDCP・ネット、
当選枠 `2-4`、グロス下位3名、罰金の内訳、馬券の配当700円、GTOの全額返金が
すべて公式値と一致**しました。`haiya.js` に固定してあります。

内蔵マスタの（実在のコース）のパー配分も、集計表の実物と完全一致を確認済みです。

---

## 9. 個人情報の扱い（9月4日に発生した事故）

**公開中の `pc.html` に、実在の氏名4名と実在の生年月日1件が入力例として載っていました。**
9月3日15:12のコミットから、公開期間中は誰でも閲覧できる状態でした。
スター0・フォーク0の個人リポジトリで公開は1日程度だったため、実際に取得された可能性は低いと見ています。

### 対処したこと

- 入力例の実名を架空名に差し替え、実在の生年月日を無関係な日付に変更
- 使い方タブの個人名（「瀬川さんと突き合わせて」）を役割名に変更
- **`privacy.js` を新設。** 公開前に通せば、同じ形の事故は防げます

`privacy.js` は「実名リストと照合する」のではなく「**素性を説明できる語しか置かれていないこと**」を
確かめる作りです。テスト自体が実名の一覧になるのを避けるためです。日付には1つずつ理由を書かせています。

### git履歴について

**リポジトリを非公開にしても、履歴には実名が残ります。** 消すなら履歴を書き換えるか、
リポジトリを作り直す必要があります。コミット数が少ないうちに済ませるのが確実です。

**2026-09-18 16:37：作り直しを完了しました（済）。** コミット2件・38ファイル・履歴の実名0件を照合で確認済み（4.22）。
以下は決めたときの記録です。

**2026-09-18：作り直すと決めました。** 判断しないまま公開に戻っており、履歴の6コミットに実名4名（姓名とも）と
実在の生年月日1件が残っていることを確かめました（最新版には0件）。ブランチは main のみ、Issue・PR・リリース0件、
フォーク0・スター0なので、失うのはコミット履歴だけです。手順は `CompeMasterPro_リポジトリ作り直し手順.md`。


### 氏名の対応表

`氏名対応表_社内限り.txt`（ナレッジ内）。実名と、テスト用の架空名の対応表です。
**GitHubには置かないでください。**

架空名は適当に選んでいません。読み取りテストが本番より易しくならないよう、
**同姓3名・1文字の姓・1文字の名3名・異体字**という構造を維持し、
実名の文字を姓名とも1文字も引き継がないようにしてあります（`namemap.py` で自動検査）。

---

## 10. ナレッジの構成（2026-09-18、v58の時点）

| ファイル | 役割 |
|---|---|
| `CompeMasterPro_引き継ぎ_2026-09-18_v58.md` | この文書。全体像 |
| `CompeMasterPro_検証スイート_1ファイル版.md` | 25本・2179項目。展開して実行できる |
| `CompeMasterPro_実機確認シート_v58.md` | iPhone・本番の機材でしか確かめられない項目 |
| `照合用_md5_v58.txt` | GitHubに上げたあとの照合用 |
| `pc.html` / `index.html` / `404.html` / `sw.js` | 配布物の本体。**GitHubが非公開のときはこれが正本** |
| `README.md` / `CONTRIBUTING.md` | リポジトリの入口と開発の手引き（GitHubのものと同じ） |
| `IMG_8530.png` | **2026年の当日集計表。実データ再現の原典** |
| `氏名対応表_社内限り.txt` | 実名と架空名の対応。社内限り。**GitHubには置かない** |
| `読み取りテスト用_*.pdf` | 技能賞・集計表の入力練習用（架空データ） |
| `全国コ゛ルフ場マスタ_20260908.xlsx` / `中部5県…_20260903.xlsx` | マスタ原本 |
| `コ゛ルフ場マスタ_引き継ぎ_メモ_20260908.md` / `claude_ゴルフ場マスタ_収集ルール…md` | マスタの収集手順と残り |
| `馬券_GTO集計表_*.xlsx` | 2026年の実績記録と、スコア連携版 |
| `CompeMasterPro-Redesign_dc.html` / `support.js` / 画面の画像 | 別アカウントから届いたデザイン案（取り込み完了。4.6〜4.17） |

`pc.html` をナレッジに置いているのは、**リポジトリを非公開にするとPagesが止まり、
手元が唯一の正本になる**ためです。9月4日にこの状況が実際に起きました。

---

## 11. 次にやること

### 未確認の宿題

1. ~~ゴルフ場に集計表のPDFを頼めるか確認する~~ → **取り下げ（9/18）**。打ち込みの検算で足りる（4.22）
2. スマホ・iPadで、**参加者タブの氏名がフルネームで見えるか**（9/4に列幅を修正。要実機確認）
3. **打ち込みの検算を実際に使ってみる。** 16名で何分かかるか、○×が読みやすいか
4. ~~git履歴に残る実名を消す~~ → **済（9/18 16:37、作り直し完了）**

### 本番前

5. 当日は**必ず従来の方法と突き合わせる**。アプリが優勝と言っても、それだけで表彰しない
6. **対象ホールの設定を確認する**（初期値は（実在のコース）に合っていません。警告が出ます）
7. USBに `pc.html` を入れて持っていく（控え）

### 積み残し（2026年は発生せず未確定）

- 選抜者が3位タイ（2026年は1票差まで迫った）→ **手動指定で会長が決める仕組みは実装済み**

### 9月16日時点で残っていること

**次に手を付けるもの**

1. ~~「次にやること」を1行出す~~ → 済（4.7の段階1）
2. ~~デザイン案の段階2~~ → 済（4.8）
3. ~~デザイン案の段階2（骨格）・段階3（入口）~~ → 済（4.10・4.11）
3a. ~~デザイン案の段階4（準備）~~ → 済（4.12）。「スタート表を印刷」は印刷したかを確実に知れないので判定にせず、
    「スタート時刻を入れる」を手順に足した
3c. ~~デザイン案の段階5（当日と結果）~~ → 済（4.13）
3e. ~~デザイン案の段階6（予想入力）~~ → 済（4.14）
3f. ~~デザイン案の段階7（発表）~~ → 済（4.15）
3g. ~~デザイン案の段階8（スマホ）~~ → 済（4.16）
3h. ~~デザイン案の段階9（仕上げ）~~ → 済（4.17）。**デザイン案の取り込みは完了**
3i. **実機確認**（同梱の実機確認シート）。iPhone・本番の機材でしか確かめられない項目。
    **v54でスコア入力の導線を直したので、「ハンディを直接入れる」「棄権・ノーカードの印」も確かめてください**
    **v55で集金の差額とキャンセル料を足したので、差額のカード・「繰越金で埋める」・集金表の印刷（A4縦1枚に右端まで）も確かめてください**
    **v57でPC・タブレットを1本のスクロールにしたので、白い帯が出ないこと・サイドバーが本文と一緒に流れること・タブレット幅のロゴでホームへ戻れること・スマホの上の帯は固定のままであることを確かめてください**
3d. ~~容量の判断~~ → 済。上限を700KBに上げた（4.12）。v48で556KB
3b. ~~4.11の「仮に決めたこと」6点の確認~~ → 済（すべてそのまま採る）

**確かめること**

4. **実機での確認** — 3段階の切替と「次にやること」／大会設定の畳み（iPhoneの `details`）／集金の一連（原資・実費・車・丸め）／音／全国版の検索／
   ~~v47：ホームの「最近のファイル」から選んで開く・作成画面での日本語入力・技能賞カードの「合わせる」ボタン~~ → 済（iPhone、4.11）／
   ~~v48：組合せで名前を長押しして運ぶ・押して行き先を選ぶ・参加者の列の切替・スタート時刻の一括入力~~ → 済（iPhone、4.12）／
   **v49：スコア入力（iPhone の数字キーで「次へ」「完了」を押したときの動き・日本語入力の欄との行き来）・順位表の印刷（賞の列が紙に収まるか）**／
   **v50：予想入力（枠・人のボタン、− ＋、締め切りと解除）・集金の「締め切る前」の知らせ**／
   **v51：本番と同じ機材（ノートPC＋プロジェクター）で、操作パネルを手元・発表をプロジェクターに分けて全画面にできるか、音、2列の配置の読みやすさ。
   Claude のプレビューで `pc.html` が崩れずに開くか（4.15の「公開前に見つけて直したこと」1）**／
   **v52：iPhone で、上部の低さ、大会ハブの「すべての画面」、1人ずつのスコア入力（数字キー・次の欄へ・一覧との行き来）、順位のカード**／
   **v53：実機確認シート（27項目）にまとめた。iPhone・本番の機材（プロジェクター）・印刷**
5. ~~ゴルフ場に集計表のPDFを頼めるか確認~~ → 取り下げ（9/18）
6. ~~集金したあとに前提が変わったときの差額管理~~ → 済（4.19、v55）
7. **会員制コースの取りこぼし補完**（愛知以外の46都道府県が未突合。マスタの引き継ぎメモ参照）
8. ~~git履歴に残る実名を消す~~ → **済（9/18 16:37）**

> 9月3日時点の積み残し5件のうち4件は9月4日に確定しました。詳しくは「6. 予想ゲームのルール」を参照。

### 将来の拡張候補

- ゴルフ場マスタを山梨・北陸へ拡張（手順は収集ルールMDに記載）
- キャロウェイ方式（別体系のハンディ。実装より検証の負荷が大きい）
- 投票用紙ファイルに残る不具合の修正（`C24` の `#VALUE!`、ルールシート `B15` の誤記）

---

## 12. 新しいチャットへの最初の一言（例）

> CompeMaster Pro（ゴルフコンペ総合運営システム）の続きです。引き継ぎMDと検証スイートはナレッジにあります。まず◯◯をしたいです。

引き継ぎMDと検証スイートがあれば、前提の説明は不要です。

**読み取りアプリの再開を提案されたら、「4. 読み取りアプリを廃止した理由」を読み返してください。**
一度検討し尽くした道です。
